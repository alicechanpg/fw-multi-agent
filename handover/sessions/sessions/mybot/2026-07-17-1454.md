# Session Handover (mybot) — 2026-07-17

> 本 session（`ebbd61cf`）主線＝**建立 SOP 治理系統**（P0 完成 + 設計經 opus 稽核升 v2）。
> 下方 FWP-814 是 7/16 留下、**至今仍未完成**的工作，原樣保留。

## 本次（7/17）完成

### 1. /audit-review — 找出 3 個可改行為
1. **對外送出前未驗證**（送 Teo 的 GitHub URL 是壞的）→ 送 Slack/JIRA 前用 `gh api ... html_url` 取權威 URL。
2. **把訓練知識當硬體事實**（7/16 反覆被糾正；最貴一次是在未驗證 repo 上派了 2 個 subagent）→ 派 subagent 前先釘死 source-of-truth。
3. **重找已知資訊 + 讀錯字硬做**（excel PID/VID 重搜、audio/audit 誤讀）。
- **關鍵發現**：這 3 條的 memory **早就存在**（evidence_based 建於 5/05、stop_searching 建於 3/26）卻照樣犯 → 證明「寫下規則」≠「照做」，**行政控制無效，要靠工程控制（hook）**。

### 2. P0 完成並驗證 — 可評估的 metric 層
- `handover/scripts/metrics.py`（新）：純函式算 KPI，永不 raise。
- `session-digest.py`（改）：Stop 時算 metric → 寫進 digest + `handover/audit/metrics.jsonl`（try/except 包住，不阻斷關機）。
- `audit-report.py`（改）：`/audit-review` 多一段 KPI 趨勢表。
- **驗證**：對 7/16 真實 transcript 算出 output=1,623,358 / cache_read=232M / turns=922 / model=claude-opus-4-8，與手算逐項一致；py_compile 三檔通過；dummy 空 Stop payload 實跑 exit=0。
- metrics.jsonl 已 backfill 2 筆真實 session（用其真實日期）。

### 3. 設計文件 + 獨立 opus 稽核 → v2
- Spec：`docs/superpowers/specs/2026-07-17-sop-governance-p0-metrics-design.md`（已 push 到 fw-multi-agent，commit `1ce97ce`）。
- Google Doc（Alice 讀）：https://docs.google.com/document/d/1YmEIX0YqhWLlz66AnrIelwq7LG1d_tYJ2DO806sZKYc/edit
- 已 Slack Alice 兩則稽核結論。
- **獨立 opus 稽核 Verdict: needs-changes**，v2 已納入（見 spec §7）：
  - 貴 model 用錯位置：一次 opus 抽查 ≈ 直接用 opus 做完整件事 → **判斷移到「捕捉當下」驗證**。
  - **P1 改成 capture-first**（捕捉管道 + 存入當下驗證），不是比對引擎；存錯比沒存更糟。
  - registry = 單點故障 → 每筆 fact 要 owner/provenance/scope/TTL；COM/PID 等高變動事實**現場探測不快取**。
  - escalation 改**可觀察事件觸發**（registry miss / hook fail / 高風險），非模型自評信心（unknown-unknowns 不會觸發）。
  - 不可逆動作（flash/Jenkins/push/DFU）**100% 硬 gate**，不抽樣。
  - **KPI 修正（我原提案有誤）**：「escalation-to-user 下降」會被**沉默作弊** → 改用 **repeat-escalation rate + 固定抽樣錯誤率**。

### 4. P1 包袱盤點（已完成，未動手改）
- memory 67 檔：「證據/驗證」群 **7 個檔＝同 1 條規則的情境切面 → 可 7 併 1**；「別停下來」3 併 1。
- **COM/PID 事實在 3 處衝突**（COM7 vs COM19）→ registry 要以 **PID/VID 為 key**，COM 號不當常數存。
- MEMORY.md 索引覆蓋 88%，**8 個孤兒檔沒進索引**（含硬體事實 com19/com50/check_hardware_pid）。
- frontmatter 有**兩種 schema 並存**（頂層 type: vs 巢狀 metadata:）→ 遷移前要統一 parser。
- fw-multi-agent 結構包袱：雙層路徑 `scripts/scripts/`、`audit/audit/`（成因＝sync 腳本 `cp -r src/ dest/` 在 dest 已存在時的巢狀，L37-40）；**無 .gitignore** → `__pycache__`/`TEST-SID.jsonl` 被 tracked；98.3% tracked 檔是高頻 auto-handoff 噪音。

## 未完成 / 進行中

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| **P1 — capture-first registry** | 設計就緒、未動工 | ①先量 workload 分布確認捕捉 ROI（韌體/研究疑似 long-tail，ROI 可能不成立）②設計 capture 管道 ③依盤點遷移 |
| **結構清理** | 未做 | 先加 `.gitignore` → 修 sync `cp -r` 巢狀 → `git rm --cached` 去雙層（開分支，屬破壞性） |
| **P2/P3/P4** | 未設計 | P1 穩了再說 |

## FWP-814 carryover（7/16 未完成，仍有效）

- **已完成**：root cause = audio anchor 假設所有 audio chip 都是 PG VID；LIVE audio 實為 C-MEDIA `0d8c:0033`。Fix `938239e` 已 push（audioPid 改 `"vid:pid"` + PG VID 優先）。
- **未完成**：

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| Jenkins #32 (`938239e`) | 7/16 building | 查結果；上機安裝需重打 `SIGN=true` |
| **LIVE `0d8c:0033` 實機驗證** | **未驗證** | 需 LIVE 在線（現接 Reactor 100） |
| **Spark EDGE `0251`** | **未驗證** | 與 LIVE `0221` 同為推測值，很可能同樣錯 |
| C-MEDIA false positive | 已緩解非根除 | 無 PG 裝置時一般 USB 音效卡仍可能被當 LIVE |
| Reactor DFU LED source vs 實機 | 待對齊 | 本機 external-loader = Power LED，實機 = WiFi+BLE 白閃 |

## 環境狀態
- `D:\mybot` = `Positive-LLC/FWP-713-fw-agent-dev`（公司 org，master）；**從未追蹤 handover/**
- `D:\mybot\fw-multi-agent` = `alicechanpg/fw-multi-agent`（**個人 repo，handover 的指定家**，main）
- ReactorFwUpdater branch `feature/FWP-814-audio-anchor` @ `938239e`
- 硬體：**Reactor 100** 在線；無 LIVE / 無 EDGE

## 給下個 session 的備註
- **Jenkins 一律 `TRIGGER_OSX=true`**；交付 build 用 `SIGN=true`
- **對外送 URL 前先用 `gh api` 核對**（本次 audit 最嚴重的一條）
- **bash 裡不要用 PowerShell heredoc `@'...'@`**——本 session 因此讓 commit subject 多了個 `@`（已 amend 修掉）。多行字串用 `printf`。
- audio anchor 的 VID **不一定是 295D**
