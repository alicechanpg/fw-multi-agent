# Session Handover (mybot) — 2026-04-21 13:26

## 狀態：FWP-774 Spark 2 自動化環境驗證完成，Nathan 接手

今日 Alice 完成 Spark 2 flash + power relay 端到端實測並交接給 Nathan。

---

## Done

### JIRA
- **建立 FWP-774** `[Agent Workflow] Spark 2 target 自動化環境（power/flash/log）`
- Comment #87087：flash 驗證（W25Q32JV loader 選擇、ST-Link UR mode）
- Comment #87091：完整指南（TL;DR + A/B/C/D 段、已知限制、失敗處理）
- Assign 給 **nathan chang**

### Flash / Build 驗證
- **ST-Link Connect Under Reset 才接得到 STM32H750**（CubeIDE 預設 Normal Connect 會「找不到 target」）
- **QSPI 必須用 `W25Q32JV_STM32H750.stldr`**，通用 `QSPI_H750XB_W25Q32_flashloader.stldr` 會 verify fail at 0x90000003 (0x20 vs 0x24)
- Jenkins SPARK_II_BOOTLOADER #3 artifact `spark-ii-external-loader.bin` 燒到 0x08000000 ✓
- **DFU 路徑也 work**（PID 295d:0232）：`dfu-util.exe -s 0x90000000:leave:mass-erase:force -d ",295d:0232" -D <fw>.bin`
- **要 UART log 必須改 `user_config.h:23`** 從 `RUNNING_MODE_APP` → `RUNNING_MODE_APP_DEBUG` 再 build
- Build: `D:\mybot\git\tool\build-spark2-headless.bat` 1m17s 成功

### Power key relay 實測
- **開機和關機都是短按**（600ms+ release），不是 long hold
- Code ref: `PowerManager.cpp:341-351` — 同一 `EVENT_BUTTON_POWER_SHORT_PRESS` event 在 OFF 觸發 ON，在 ON 觸發 OFF
- **開機：Pulse COM3 1500ms ✓**（實測看到 `[ ***** POWER ON ***** ]` + audio/BT init）
- **關機：Pulse 1500ms 會觸發 POWER OFF event 但 adapter only 會 auto re-on**
  - Firmware 完整跑完關機 sequence (log 到 `DataManager file operations are completed.`)
  - 但 MCU VDD 持續 → MCU reset 後 bootloader reboot → 硬體某個 latch 讓 PWR_KEY 讀為 pressed → auto POWER ON
  - 推測（未驗證）：需接電池或第二 relay 切 adapter 才能乾淨關機

### Memory 更新
- 新增 `reference_spark2_log_patterns.md` — 關鍵 log pattern，其中 `DataManager file operations are completed.` 是關機執行偵測點（Alice 指定）

---

## Pending

| Item | Status | Next Step |
|------|--------|-----------|
| FWP-774 scope 三個待 PM 決策 | In FWP-774 comment | Nathan 決定 adapter 斷電方案（電池 / 第二 relay / firmware command） |
| DFU mode 進入自動化 | Manual only | 待 Nathan（今天 Alice 手動進 DFU） |
| 接電池後關機行為 | 未驗證 | 電池電量要夠（今天 2.88V 太低） |
| `flash-spark2-stm32.ps1` 改寫 | 未完成 | Alice 先前要求但今天 subagent review 中斷後未續。目前 stub 仍是 TODO，實際用到的命令在 FWP-774 comment 內 |
| `spark-ii-fw/Source/Common/config/user_config.h:23` | **Modified (APP_DEBUG)** | 保留給 debug 用；若要燒 release 版要先 revert |

---

## Environment

### Git
- `D:\mybot\` branch: master（多個 .claude / ai-monitor / src 改動，大部分是舊的）
- `D:\mybot\git\spark-ii-fw\` branch: **feature/SG-7-remote** HEAD f83b907
  - Local modification: `Source/Common/config/user_config.h` (RUNNING_MODE_APP → APP_DEBUG)
- `D:\mybot\fw-multi-agent\` 準備 push 本次 handover

### Hardware
- **Spark 2** on Alice 桌上，adapter 20V only（battery 2.88V，無效）
- **STLINK-V3SET** 接 Spark 2 SWD
- **USB Relay (LCUS-1 CH340)** on COM3 @ 9600，接 power key MOS GPIO（Richard 接線）
- **STM32 Debug log** on COM4 @ 921600 (UART5 → STLink VCP)
- MCU 最後測量 3.31-3.33V，最後一次 pulse 後狀態未知（可能 POWER_MODE_ON）

### 已燒韌體
- Bootloader @ 0x08000000: Jenkins SPARK_II_BOOTLOADER #3
- Firmware @ 0x90000000: spark-ii-fw Debug build (**APP_DEBUG mode**, 922036 bytes, with UART log)

---

## Notes for next session

### 不要做的事
- **不要 revert user_config.h:23** — 除非要燒 release 版本測試
- **不要用通用 QSPI loader** — 只用 `W25Q32JV_STM32H750.stldr`
- **不要改 flash address** — bootloader 0x08000000 internal, firmware 0x90000000 QSPI

### 回來時可做的事
- 若 Nathan 接了電池，可以重新測「乾淨關機」(長按 pulse 1500ms 後 firmware 是否停在 POWER_OFF 不自動 boot)
- 若 Alice 要 release build 給量產，先 revert user_config.h:23 再 build
- 改寫 `flash-spark2-stm32.ps1` 成可用 script（參數可用 Mode=firmware/full/bootloader，詳見今日 subagent review 建議）

### 溝通記錄
- Alice 糾正了我三次：
  1. COM4 是 STM32 log 不是 relay（relay 在 COM3）
  2. Relay 不是接 power key 而是 MOS GPIO（standby 喚醒用）
  3. 開機和關機都是短按不是 long hold（我原以為 LONG_HOLD 才關機）
- Alice 偏好：不要 try-error，先查 spec/code
- Nathan 有問題要直接找 Alice（已寫在 FWP-774 comment）

### 今日觸及的 JIRA
- FWP-774（今天新建，Alice 建立 + 兩個 comment + assign Nathan）
- 無其他 ticket 動到
