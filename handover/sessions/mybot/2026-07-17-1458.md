# Session Handover (mybot) — 2026-07-17

> 本 session（`ebbd61cf`）主線＝**建立 SOP 治理系統**：P0 完成、設計經獨立 opus 稽核升 v2、P1 spec + 實作計畫就緒。
> **下個 session 可直接執行 P1 實作計畫**（見「下一步」）。
> FWP-814 是 7/16 留下、仍未完成的工作，原樣保留在最後。

---

## 下一步（下個 session 從這裡開始）

**執行 P1 實作計畫**：`docs/superpowers/plans/2026-07-17-p1-source-of-truth-registry.md`（705 行，5 個 TDD task，已 push `b9330b1`）

- 執行方式**待使用者選**：Subagent-Driven（每 task 一個新 subagent，推薦）或 Inline（本 session 分批 checkpoint）。
- 環境已驗證：**Python 3.13.3 + pytest 9.1.1 可用**；`handover/` 原無測試慣例 → 計畫會新建 `handover/tests/` + `conftest.py`。
- **計畫刻意不自動刪 memory 檔**（見下方安全前提）。

---

## 本次（7/17）完成

### 1. /audit-review — 3 個可改行為
1. **對外送出前未驗證**（送 Teo 的 GitHub URL 是壞的）→ 送 Slack/JIRA 前用 `gh api ... html_url` 取權威 URL。
2. **把訓練知識當硬體事實**（7/16 反覆被糾正；最貴一次是在未驗證 repo 上派 2 個 subagent）→ 派 subagent 前先釘死 source-of-truth。
3. **重找已知資訊 + 讀錯字硬做**。
- **關鍵發現**：這 3 條的 memory **早就存在**（evidence_based 建於 5/05、stop_searching 建於 3/26）卻照樣犯 → **行政控制（寫規則）無效，要靠工程控制（hook）**。這成為整個系統的理論支點。

### 2. P0 完成並驗證 — 可評估的 metric 層
- `handover/scripts/metrics.py`（新）+ `session-digest.py`／`audit-report.py`（改）。
- **驗證**：對 7/16 transcript 算出 output=1,623,358／cache_read=232M／turns=922／model=claude-opus-4-8，與手算逐項一致；py_compile 三檔過；dummy 空 Stop payload 實跑 exit=0。
- `metrics.jsonl` 已 backfill 2 筆真實 session。**下個 session 關機起 KPI 表自動長**。

### 3. 設計 v2（經獨立 opus 對抗稽核，Verdict: needs-changes）
- Spec：`docs/superpowers/specs/2026-07-17-sop-governance-p0-metrics-design.md` §7 是稽核修正。
- Google Doc（Alice 讀）：https://docs.google.com/document/d/1YmEIX0YqhWLlz66AnrIelwq7LG1d_tYJ2DO806sZKYc/edit
- 已 Slack Alice 兩則結論。
- **稽核抓到的承重缺陷**：
  - 貴 model 用錯位置：一次 opus 抽查 ≈ 直接用 opus 做完整件事 → **判斷移到「捕捉當下」**。
  - registry = **單點故障**（三層檢查共用同源，一筆過期資料一起錯）→ 每筆 fact 要 owner/provenance/scope/TTL。
  - cheap model **不知道自己不知道** → escalation 改**可觀察事件觸發**，非自評信心。
  - **不可逆動作（flash/Jenkins/push/DFU）100% 硬 gate**，不抽樣。
  - 改善層自己沒人審 + 被指標帶偏 → diff 需人審 + 可回滾 + 防作弊反向指標。
  - **KPI 修正（我原提案錯了）**：「escalation-to-user 下降」會被**沉默作弊** → 改用 **repeat-escalation rate + 固定抽樣錯誤率**。

### 4. Workload 分布量測 → **P1 GO**
報表：`handover/audit/workload-distribution.md`（28 session／1120 則真人 prompt）

| 證據（regex 抽取，無選擇偏差） | 跨≥2 session 重現率 |
|---|---|
| JIRA ticket（＝「問題」代理） | **22%** → 每次都是新問題（long-tail）|
| PID/VID（＝「事實」代理） | **71%**，90% 提及來自會重現的 |

→ **永遠不問同一個問題，但一直需要同一批事實**。capture ROI 成立，但要 **key 在事實上，不是問題上**。核心約 **50 個實體** → registry 是「一張表」不是知識庫。
> ⚠️ 誠實標註：實體重現 ≠ 同一事實被重複需要（代理指標）。keyword 類（90%）因清單人工挑選**有選擇偏差、不採信**。COM 樣本僅 5 次無訊號。
> ⚠️ 第一版量測有 word-boundary bug（`ble` 吃到 problem/possible），已修正重跑；勿引用舊的「98.7%」。

### 5. P1 溫啟動發現（改變風險評估）
稽核擔心「registry 空 → 使用者被永久課稅」。但盤點證明**事實已被捕捉過**（`reference_*.md` 14 檔 + 3 個錯置的）→ **頭部事實已在手，P1 主要是遷移不是從零問起**。

### 6. P1 spec + 實作計畫
- Spec：`docs/superpowers/specs/2026-07-17-p1-source-of-truth-registry-design.md`（`e2eeb13`）
- Plan：`docs/superpowers/plans/2026-07-17-p1-source-of-truth-registry.md`（`b9330b1`）
- **已定案的兩個決策（使用者選的）**：
  1. **捕捉驗證：opus 寫+自驗**，只在「衝突／高風險（flash/PID/DFU/不可逆）／信心不足」時回問使用者；其餘走週報 digest 事後抽看（防 approval fatigue）。
  2. **registry 取代 `reference_*.md`**（單一真相），避免兩份真相分歧（COM7/COM19 衝突的成因）。

### 7. P1 包袱盤點（已完成，未動手改）
- memory 67 檔：「證據/驗證」群 **7 檔＝同 1 條規則的情境切面 → 可 7 併 1**；「別停下來」3 併 1。（**SOP 整併不在 P1 範圍**，另案）
- **COM/PID 事實在 3 處衝突**（COM7 vs COM19）→ registry 以 **PID/VID 為 key**，COM 一律 `volatile:true` + probe。
- MEMORY.md 索引覆蓋 88%，**8 個孤兒檔沒進索引**（含 com19/com50/check_hardware_pid）。
- frontmatter **兩種 schema 並存**（頂層 `type:` vs 巢狀 `metadata:`）→ 遷移前要統一 parser。
- fw-multi-agent 結構包袱：雙層 `scripts/scripts/`、`audit/audit/`（成因＝sync 腳本 `cp -r src/ dest/` 巢狀，L37-40）；**無 .gitignore** → `__pycache__`/`TEST-SID.jsonl` 被 tracked；98.3% tracked 檔是 auto-handoff 噪音。

---

## ⚠️ 安全前提（P1 執行時必守）

**`C:\Users\alice\.claude\projects\D--mybot\memory\` 不在 git 版控 → 刪除不可回復。**
遷移順序不可跳：**先備份 → 逐檔完整讀取遷移 → 覆蓋率對照表證明每條事實都有對應 key → 全部 covered 才可刪 → 更新 MEMORY.md 指向 registry**。
分流不是全搬：`reference_google_drive_visibility` 這類偏好註記**不進 registry**；分流清單須經使用者確認。

## 未完成 / 進行中

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| **P1 實作** | 計畫就緒、未執行 | 選執行方式 → 跑 5 個 TDD task |
| **memory 遷移/刪除** | 未做 | 人工關卡，須備份 + 覆蓋率背書 |
| **SOP 整併（7併1/3併1）** | 未做 | P1 之外的另案 |
| **結構清理** | 未做 | 先加 `.gitignore` → 修 sync `cp -r` 巢狀 → `git rm --cached` 去雙層（開分支） |
| **P2/P3/P4** | 未設計 | P1 穩了再說 |

---

## FWP-814 carryover（7/16 未完成，仍有效）

- **已完成**：root cause = audio anchor 假設所有 audio chip 都是 PG VID；LIVE audio 實為 C-MEDIA `0d8c:0033`。Fix `938239e` 已 push。
- **未完成**：

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| Jenkins #32 (`938239e`) | 7/16 building | 查結果；上機安裝需重打 `SIGN=true` |
| **LIVE `0d8c:0033` 實機驗證** | **未驗證** | 需 LIVE 在線（現接 Reactor 100） |
| **Spark EDGE `0251`** | **未驗證** | 與 LIVE `0221` 同為推測值，很可能同樣錯 |
| C-MEDIA false positive | 已緩解非根除 | 無 PG 裝置時一般 USB 音效卡仍可能被當 LIVE |
| Reactor DFU LED source vs 實機 | 待對齊 | 本機 external-loader = Power LED，實機 = WiFi+BLE 白閃 |

## 環境狀態
- `D:\mybot` = `Positive-LLC/FWP-713-fw-agent-dev`（公司 org，master）；**從未追蹤 handover/**
- `D:\mybot\fw-multi-agent` = `alicechanpg/fw-multi-agent`（**個人 repo，handover + 本專案文件的家**，main）
- ReactorFwUpdater branch `feature/FWP-814-audio-anchor` @ `938239e`
- 硬體：**Reactor 100** 在線；無 LIVE / 無 EDGE
- Python 3.13.3、pytest 9.1.1

## 給下個 session 的備註
- **對外送 URL 前先用 `gh api` 核對**（本次 audit 最嚴重的一條）
- **bash 裡不要用 PowerShell heredoc `@'...'@`**——本 session 因此讓 commit subject 多了個 `@`（已 amend 修掉）。多行字串用 `printf`。
- **console 是 cp950**：腳本不要把中文印到 stdout；報表寫檔用 Read 讀。
- **Jenkins 一律 `TRIGGER_OSX=true`**；交付 build 用 `SIGN=true`
- audio anchor 的 VID **不一定是 295D**
- 本 session 未動 JIRA（此工作非 ticket 驅動）；Slack/Atlassian MCP 中途曾斷線
