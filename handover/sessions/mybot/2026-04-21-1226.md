# Session Handover (mybot) — 2026-04-21 11:03

## 狀態：Phase A 建置完成，等硬體接回測試

Alice 昨日委任 Claude 自主完成 FWP-664 feature（修 code + build + flash + test）。
昨天 Claude 誤解「不要自己修 code」規則只寫報告；今天修正理解、實際動工。

記憶規則已更新：`feedback_no_code_changes.md` 明確只限 migration/cherry-pick。

---

## 本次完成

1. **Stash 昨日 working tree 實驗** — `git stash` "alice-debug-experiment-2026-04-18"
   - 內含：ble.c:940 save MAC 被註解測試、debug log 升級、START_SCAN 少一行 `use_mac_filter=false`
   - 清 stash: `git stash list` → `git stash pop`
2. **ESP32 ble.c edit** — 在 `ble_nvs_save_paired_mac()` 內加 NVS 寫入 timing log
   - `nvs_set_blob` 前後、`nvs_commit` 前後各 `esp_timer_get_time()`
   - Log: `[FWP-664] nvs_set_blob=%lld us, nvs_commit=%lld us`
   - 目的：驗證 Alice 的「NVS write 卡 BT」懷疑（直接量測耗時）
3. **ESP32 build 成功**（exit 0）
   - 產物: `D:\mybot\git\pg-reactor-esp32-wifi-bt\build\wifi_and_bt_core_on_esp32.bin` (0x1514b0 bytes)
   - 1 pre-existing warning（未用 `remote_filter_battery_service_uuid`，與 FWP-664 無關）
4. **2 輪 subagent review 確認 edit 策略**（per AI Collab L1 rule）

## 硬體 Blocker

**ESP32 沒插上**（11:03 偵測）— 只有 STM32 STLink (COM4) + USB Relay (COM3)。
Memory 說 ESP32 應在 COM19，但未連線。

→ Phase 1 smoke + Phase 2 reboot 測試**卡住**，等 Alice 插回 ESP32 才能繼續。

## Alice 回來後建議操作

1. 插 ESP32（COM19 應該會回來）
2. 跑 flash（Claude 會做，或手動）:
   ```bash
   # 建議先 erase_flash 起乾淨 NVS
   D:\mybot\git\tool\esptool-v4.5.1-win64\esptool.exe -p COM19 erase_flash
   # 然後 flash 新 firmware
   powershell -File D:\mybot\git\tool\flash-esp32.ps1 -Port COM19
   ```
3. Attach logger 抓 boot log
4. Phase 1 smoke: 長按 BT 5s 配對，確認 log 出現：
   ```
   [FWP-664] nvs_set_blob=XXX us, nvs_commit=XXX us
   Paired MAC saved: xx:xx:xx:xx:xx:xx
   ```
   - 若 `nvs_commit < 100000 us` (100ms) → NVS 不卡 BT，Alice 懷疑證偽
   - 若 `>500ms` → 需改 defer 到 WRITE_DESCR_EVT 之後
5. Phase 2 測試：USB relay power cycle → 驗證 boot 後自動 MAC filter scan → 連上相同 MAC

## 未完成 / 待 Phase A 通過才動

### Phase B：STM32 GATTC branch (`feature/FWP-664_auto_connect_GATTC` commit `6c9b2a1` + `d5fe71b`)

**確認的 3 處 bug（原 code 在 Alice 自己的 commit 裡）：**
- 3 處呼叫 `start_stop_scan_pedal(PEDAL_CMD_START_SCAN)` (0x01) **應為 `PEDAL_CMD_RECONNECT` (0x10)**：
  - boot `spi_command_process_task` 讀 `FILE_BLE_AUTO_CONNECT` 後
  - case 0x80 (disconnect) 的 auto-reconnect 分支
  - case 0x83 (scan timeout) 的 auto-reconnect 分支
- 3 處都設 `set_bt_long_pressed(1)` — 模擬手動配對，應移除（auto 流程不該偽裝手動）
- 0x83 handler 缺 `bt_long_pressed` guard（AI 建議的 edge case 防護）

**設計但未實作的 backoff**（P1）:
- `scan_timeout_count`: 1-3 立刻 / 4-10 delay 30s / >10 delay 120s
- connected (0x81) 時 reset count=0

**STM32 build 環境在線**：STLink COM4 接著，理論可 build + flash；但 Round 2 建議先通過 Phase A 再動 Phase B，避免多變數改動混淆 debug。

## 環境狀態

- ESP32 branch: `feature/FWP-664_mac_auto_reconnect` HEAD `4707553`
- ESP32 working tree: 1 file modified (ble.c, timing log)
- ESP32 stash: 1 (alice-debug-experiment-2026-04-18)
- ESP32 build: ✅ OK, bin ready
- ESP32 flash: ❌ 硬體未連
- STM32 reactor-fw branch: `develop`
- STM32 reactor-50-100-fw branch: `FWP-664-rebase`

## Design Docs
- `D:\mybot\docs\FWP-664-user-scenario-design.md`
- `D:\mybot\docs\FWP-664-revised-sequence-diagrams.md`
- `D:\mybot\docs\FWP-664-ble-sequence-diagrams.md`

## 今日 AI Collaboration 記錄

- **Round 1 subagent**: review 修復計畫方向 → 建議 E→A→B 順序、一次一個變數、不要 ESP32+STM32 一起改
- **Round 2 subagent**: review 具體 edit diff → 建議 timing log 放 `ble_nvs_save_paired_mac()` 內分開量 set_blob 和 commit、不要 over-instrument、erase_flash 起乾淨狀態、Phase 1 smoke 分 NVS-write 和 reboot-reconnect 兩段

兩輪都收斂到同一結論，信心度高。
