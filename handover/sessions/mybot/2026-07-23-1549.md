# Session Handover (mybot) — 2026-07-23 15:48

## 本次 session（2026-07-23 15:45–15:48）：每日 Audit digest 解讀 — cost-down + Alice 可幫忙的事（純分析，無變更）

Alice 問「我凌晨的 metric Slack 訊息有什麼她可以幫忙的」＋中途插話「什麼可以再 cost down」。

**背景**：03:24 自動發到 Alice DM 的「每日 Audit Review 2026-07-22」digest（TS `1784748241.023549`）。近 7 場 vs 前 7 場：output/prompt 11.7k→12.7k(+8%)、**tool calls/prompt 2.1→2.9(+37%)**、插話/prompt 0.29→0.49(+69%)、tool 失敗率 0%（280 筆從未失敗）。

**已回報（無檔案變更）**：
- **Alice 可幫忙的 3 件**：① 批准「Jenkins 輪詢→背景等通知」改法 ② 定義什麼算「tool 失敗」（0% 疑為分類太寬鬆/漏記，非真零失敗）③ 確認「插話率」維持只觀察、不當 KPI
- **cost-down 排序**：**#1 Jenkins build 狀態輪詢**（sleep+重複 `jenkins.sh info`，多場 session `3a4e52a3`/`cee7023b`/`a79d28d9` 重複 3~10 次，是 tool_calls +37% 主因）→ 改背景 task 等通知；#2 停止重查已知資訊（token/DFU/COM 讀 memory）；#3 大型 tool 輸出用 limit/concise（本場讀 DM 一次吐 82k 字元）
- **不建議**：降 agent 模型（違反「opus 不降級」規則，省 token 不划算）
- **已驗證**：`handover/scripts/jenkins.sh` 只有 `info`/`queue`/`console`，**無 wait 子指令** → 輪詢是 session 內手動 loop（grep 驗證）

**待 Alice 拍板**：
- [ ] #1 Jenkins 輪詢改背景等通知（要不要直接做，低風險可逆）
- [ ] 「tool 失敗」的定義

---

## 前次任務（2026-07-22 17:00 session）：FWP-840 Reactor Windows Updater 在 Update Wireless Module 失敗 — 跟 QA 要 log

- **Ticket**：FWP-840（reporter: Davis Ho / QA，assignee: Alice）；Reactor 100 + Win11，Updater 顯示版本 0.1.4.170 | 685
- **已做**：從 source 驗證 updater log 位置並 comment 到 FWP-840 跟 Davis 要兩平台 log（comment id 92598, 17:18）
  - Log 路徑 = `<Documents>/PositiveGrid/reactor-100-updater.log`（Main.cpp:111-121；folderName ProductConfig.hpp:186；JUCE mac `~/Documents` juce_Files_mac.mm:196、win CSIDL_PERSONAL juce_Files_windows.cpp:719）
  - 1MB rotate → `.bak`（Main.cpp:123-134）→ 已提醒 QA 兩檔都要、先別重跑 update
- **下一步**：等 Davis 提供 log → systematic-debugging Phase 1（讀 log 實際錯誤；候選比對點：EspWrapper/Subprocess 64KB buffer + 120s timeout（registry reported）、esptool 失敗輸出）

---

## 進行中（16:40 session 續）：Reactor OTA patch 移植到 MINI2（ESP32+STM32）+ Jenkins

Alice 指示：搬 patch #1(FWP-797 512B accumulate) #2(HTTPS) #4(RAD-1454 BLE coex) #5(FWP-791 SDMMC lockdown)；跳過 #3(progress) #6(AKM7755)。**Jenkins build 過才發 PR；reviewer=KenLee-PG、assignee=alicechanpg**。`Release_Spark_FW_FOR_OTA` 要加 mini2（尚未做）。

### 已完成
- **ESP32** `pg-spark-esp32-wifi-bt` br `fix/ota-stability-port`（base develop fc56f1e）commit `4410a5b` 已 push；**本地 idf.py build 綠**
- **STM32** `spark-mini-ii-fw` br `bugfix/ota-sdmmc-lockdown-port`（base develop a6501a0）commit `b35b45d` 已 push（#5 lockdown + **stride bug fix 2048→512**）
- **重大發現 1**：mini2 STM32 OTA 寫入 stride=2048（抄 Spark 2）但 mini2 SPI hwcmd 單筆 ≤512、fw image 無 slice 重組（ESP32 SPI_Command_Slave.h:20 / STM32 SPI_Command_Master.h:14 / PGSysExCommands_Master.cpp:2417）→ **原本 OTA passthrough 燒不出正確 image**，已修
- **重大發現 2**：develop（PR #1 merge）把 `HOST_DEVICE_NAME` 改成 **DEV_GENII** → OTA 抓 genii bin、BLE 名 "Spark 2 BLE"。**未動、待 Alice 決定**；Jenkins DEVICE_NAME 參數可能會蓋掉（待看 build console 確認）
- **Jenkins**：SPARK_ESP32_FW_V2 / SPARK_MINI_2_FW 的 BRANCH choices 已加 port branch（POST 200 + readback 驗證；原 config 備份 scratchpad `*_config.orig.xml`）；已觸發 queue **2434**(ESP32, DEVICE_NAME="Spark MINI 2") / **2435**(STM32)，背景監控 `bvnmrectr`

### 全部完成（17:50）
1. ✅ Jenkins build 綠：`SPARK_ESP32_FW_V2` **#11** SUCCESS（BRANCH=fix/ota-stability-port, DEVICE_NAME="Spark MINI 2", artifact esp32-v2-fw-mini2-**rev525**.bin）｜`SPARK_MINI_2_FW` **#28** SUCCESS
2. ✅ PR 已發（reviewer KenLee-PG / assignee alicechanpg，已 API 驗證）：
   - ESP32: https://github.com/Positive-LLC/pg-spark-esp32-wifi-bt/pull/3
   - STM32: https://github.com/Positive-LLC/spark-mini-ii-fw/pull/57
   - 兩 repo 皆無 CI checks
3. ✅ `Release_Spark_FW_FOR_OTA` 已加 mini2（POST 200+readback 8 項驗證；備份 scratchpad `Release_Spark_FW_FOR_OTA_config.orig.xml`）：PRODUCT_NAME+`Spark_MINI2`、PROJ_NAME+`SPARK_MINI_2_FW`、新參數 `ESP32_PROJ_NAME`（default SPARK_ESP32_FW=舊行為；mini2 選 V2）、版本解析 mini2 走 tokens=5（檔名 `spark-mini-ii-fw-X.Y.Z.W.bin` 多一個 `-`）、上傳 `stm32_fw_mini2[_test].bin` + `esp32_fw_mini2[_test].bin`（glob `esp32-v2-fw-*.bin`）。⚠️ 中文 config POST 要 `Content-Type: application/xml; charset=utf-8`，否則 500
4. ✅ 重大發現 2 收案：DEV_GENII 是 develop 開發預設；**Jenkins build step 會 PowerShell sed 成 DEV_SPARK_MINI2**（#11 console 證實）→ 非 bug；但 sed 匹配字串寫死 `DEV_GENII`，develop 若改值 sed 會静默失效（brittle，提醒過 Alice）

### 後續（下個 session / 等 review）
- Ken review 過 → merge 兩 PR → 跑 `Release_Spark_FW_FOR_OTA`（Spark_MINI2 / SPARK_MINI_2_FW / BUILD_NO=merge 後新 build / ESP32_PROJ_NAME=SPARK_ESP32_FW_V2 / staging）→ 實機 e2e OTA
- 待驗證：mini2 bootloader 是否驗 OTA header 0x10 的產品名字串（srecord bat 會嵌 "Spark_MINI2"）；e2e 時確認
- STM32 本地 build 缺 PGLooper submodule access（SSH key/權限），BleCmdHandler/AudioService 本地沒編過、Jenkins 已覆蓋

---

## 前次任務（16:40 session）：MINI2 ESP32 OTA 實作分析（純讀碼，無變更）

Alice 問「mini2 的 OTA 在 ESP32 怎麼實現」＋「URL 有寫 macro 嗎」。已回報完整分析：

- **Repo**：`pg-spark-esp32-wifi-bt`，核心 `components/app_https_fw_ota/app_https_fw_ota.cpp`
- **URL macro 有**：`app_https_fw_ota.cpp:30-58` 用 `HOST_DEVICE_NAME` 編譯期選產品；MINI2 組在 :48-52（`esp32_fw_mini2.bin` / `stm32_fw_mini2.bin` + `_test` 版）。目前 `HOST_DEVICE_NAME=DEV_SPARK_MINI2`（SPI_Command_Slave.h:13）
- **staging 是 runtime bool**（App enter_fw_update_mode payload → `is_staging_mode`，G1MidiProtocol.cpp:2390-2397），不是 macro
- **流程**：App BLE 指令 → do_enter_fw_update_mode（:2378）→ SPI 轉 STM32 → STM32 ACK 回來（req_reply :4425-4432）才 give semaphore → main.cpp:47-51 開兩個 task：esp32_ota_task（esp_ota 標準雙 partition + rollback）→ 完成後 st_ota_task（HTTP 下載 STM32 bin，切 512B 走 SPI hwcmd 轉送，16-bit 加總 checksum，5 次 retry）
- **陷阱（已回報）**：① `HOST_DEVICE_NAME` 同時控 BLE 名+OTA URL——dev 用 DEV_GENII workaround 會連 URL 切走；② fail path 全部 `vTaskDelay(portMAX_DELAY)` 掛死（同 Reactor registry 已知行為）；③ 全程 HTTP 非 HTTPS；④ OTA 路徑無 esp_restart，新 image 下次重開機才生效
- **環境**：無任何檔案改動、無 commit、無 Jenkins 操作
- **下一步（已向 Alice 提議、未回覆）**：要不要接著看 STM32 端（spark-mini-ii-fw `PGSysExCommands_Master` 收 image 後的寫入/apply）

---

## 前一 session（15:05）：FWP-814 把 Spark MINI 2 加進 unified ReactorFwUpdater

Alice 給實機讀值 audio anchor=295D:0272、確認「進 DFU power 燈白燈恆亮」、
指定「圖用 PG 官網 Spark MINI 產品圖」「背板圖找 MINI（Gen1）實照」、
**「mini2 其實有 cdc cmd」→ 改走 Reactor 式 CDC 自動進 DFU**、
FW 來源=SPARK_MINI_2_FW + SPARK_ESP32_FW_V2、
**「用最新的 dev 當 base 長 branch」→ 已重整**。

## 已完成（全部有工具驗證）

### Registry 入庫 5 條 verified
- `USB:AUDIO_ANCHOR:SparkMINI2` = 295D:0272（hook 入）
- `USB:DFU_PID:SparkMINI2` = 0273（DFU）/ 0274（app CDC, usbd_desc.c:68）
- `HW:DFU_MODE_LED` (MINI2) = power LED **恆亮白**（PowerManager.cpp:723-726 + 實機）≠ Gen2 呼吸白
- `HW:DFU_ENTRY_COMBO` (MINI2) = 拔全部線→按住 REC→插 USB（Alice 實機程序）；app-mode 另有 Power+REC
- `PROTO:CDC_ENTER_DFU:SparkMINI2` = PGSysEx 0x7F/0x10/0x01 over CDC，**與 updater createEnterDfuCmd byte-identical**（usb_command.cpp:313-322,433-437 + MessagePacket.h:68,82）

### Code — branch `feature/FWP-814-spark-mini2`（base=origin/dev，commit `afa990a`，已 push）
- ProductConfig: `getSparkMini2Config()`（**cdcPid="0274" → 自動 CDC flow**）+ 4 處清單（audio 0272/dfu 0273/cdc 0274）
- Text.hpp: MINI2 手動 fallback 文字（拔線+REC+插USB、恆亮白）**放在 isSparkFlow() 判斷之前**（MINI2 走 non-spark flow）
- MainComponent: connect composite 2×2 寫死 → 動態 2 欄 grid（5 產品，落單置中）——原本第 5 個會被丟掉的 bug 一併修掉
- CMake×4: assets、SPARK_PRODUCTS+=spark-mini-2、manifest 名稱
- Assets ×5: PG 官網 Spark MINI 實照合成（normal/ok/warning=正面+徽章；connect/repair=Gen1 背板實照圈 USB）
- docs/FWP-814-spark-mini2.md：完整 design doc（事實表+決策+變更清單）
- ⚠️ 誤 commit 到 audio-anchor 的 `8e3ad25` 已收回（force-with-lease 回 f0a7adb）；正確 commit=cherry-pick 後的 `afa990a`

### Jenkins（兩個 job config 已改、readback 驗證）
- `REACTOR_SPARK_FW_UPDATER_WIN_OSX` + `_OSX`：加 SPARKMINI2_MCU/ESP 參數、BRANCH choice 加新 branch、
  `download_spark` 參數化 ESP job（第8參數，default SPARK_ESP32_FW）、ESP glob 改 `%SP_ESP_PAT%*`（因 mini2 artifact = `esp32-v2-fw-mini2-revNNN.bin` 不符舊 glob）、
  mini2 下載 step（→Images/spark-mini-2/）、build 描述加 SparkMINI2 行、WIN→OSX 轉發參數
- 原 config 備份：scratchpad `win_config.orig.xml` / `osx_config.orig.xml`

## 結果（18:00 收案更新）

- **Build 全綠 ×2**：WIN **#38**/OSX #28（v1.0.0.103）與 WIN **#39**/OSX **#29**（v1.0.0.104，含下述 CDC fallback）。#36/#37 失敗根因=submodule `pg-serial` 認證（BIAS_Dev wincredman token 失效）→ 兩 job **`parentCredentials=true`** 已修（readback 驗證）。BIAS_Dev 機器會反覆掉線
- **重大 HW 發現**：**MINI2 USB Audio 0272 是暫態**（只在開機後短暫列舉；PnP ghost：0272 出現 1× vs CDC 0274 出現 7+×；Alice 懷疑該台 USB audio 故障，待第二台對照/問 FW team）→ **偵測錨點改用 MINI2 獨有 CDC 0274**：新增 `cdc::findUniqueCdcPid()` 接進 4 個偵測點（commit `54c5944`）。registry 已同步（0272 降級 + 新 key `USB:DETECTION_ANCHOR:SparkMINI2`）
- **⚠️ 跨 session 注意**：v1.0.0.104 打包到的 MINI2 fw = **0.1.1.91/rev525 = 另一 session 的 OTA port branch build**（SPARK_MINI_2_FW #28、SPARK_ESP32_FW_V2 #11 期間變成「最新」）。正式包要用參數釘 `SPARKMINI2_MCU=3.2.1.90` `SPARKMINI2_ESP=524`（或等 port branch merge）
- 測試檔已備：`git\tool\release-binaries\fwp814-mini2\v1_0_0_104\Reactor Spark Firmware Updater v1_0_0_104.bat`（103 也在）

## 未完成 / 下一步

| 項目 | 下一步 |
|------|--------|
| 實機驗證 | Alice 尚未能測；跑 updater 應：CDC 0274 認出 MINI2 → 自動進 DFU（power 燈恆亮白）→ ESP32 → MCU。**觀察點：ESP32↔CDC 同 USB parent 拓樸未驗**、connect 頁型號名 |
| PR（→ dev） | 實測過後開（或 Alice 說開就開），附 design doc |
| JIRA FWP-814 comment | 同上時機 |
| 圖 | Gen1 MINI placeholder，等 design team MINI2 render |
| test-cases doc | `FWP-814-test-cases.md` 加 MINI2 欄 |
| Gen1 PSRAM burn-in 線 | **已放棄**（另 session 完成）；未執行的 scripts：git/tool/{backup-spark-mini-app,flash-spark-mini-psram-burnin}-20260722.jlink；該 GD32 secured（unsecure=全抹）注意 |

## 環境狀態
- ReactorFwUpdater 在 branch `feature/FWP-814-spark-mini2`（HEAD `54c5944`，乾淨；untracked 雜物照舊）
- Jenkins 兩個 updater job：BRANCH choice 有新 branch、SPARKMINI2 參數、parentCredentials=true

---

## 前一 session（10:50）logassert demo repo 提案 — 等 Alice 決定
- 推薦：`qa-log-patterns` 去識別化抽成公開 `logassert` 工具（CLI+YAML+pytest+Actions）
- 待答：(a) 放哪個 GitHub 帳號（不要 alicechanpg 公司 repo）(b) 命名
- 紅線：workspace/registry/token 不得推公開 repo

## 上一 session（09:41）Spark MINI2 BLE 分析 — 重啟並已修（見下）
- Alice 已回 Acow；若重啟：驗 FW 3.2.1.90 是否含 ESP32 `cb91660`、故障機 BLE sniff `0x5D29` + UART `0xA7` 比對

## 2026-07-22（傍晚）MINI2 BLE manufacturerData 截斷 — ROOT CAUSE 找到並已修（PR #2）
- Acow 回報：更新 FW 後 manufacturerData 仍錯 `02 22 ca c8 b0 f3`（少尾 2 bytes）
- **Root cause（已驗證）**："Spark MINI 2 BLE"(16字) + manu data > 31-byte legacy adv；Bluedroid flags→name→manu 順序打包、**默默截 manufacturer**（esp-idf `btm_ble_gap.c:2239`）。Spark2/LIVE/EDGE 名 ≤14 字沒事；我先前驗證 OK 是因 dev build 掛 DEV_GENII 短名
- **實機重現**：燒 develop（DEV_SPARK_MINI2）→ 空中 `LEN=6 [02 22 88 C8 B0 F3]`＝Acow 症狀；同場另一台 MINI2 也截斷、隔壁 Spark 2 完整（對照組）
- **修正（Reactor 同款 pattern）**：adv 拿掉 name/txpower/interval、name 移 scan rsp → 空中 `LEN=8 [02 22 88 C8 B0 F3 66 78]` + scan rsp 完整名 ✅
- **PR #2**：`Positive-LLC/pg-spark-esp32-wifi-bt` fix/ble-adv-manufacturer-truncation @ 36aa375 → develop（open，未 merge；repo 無 CI）
- Jenkins `SPARK_ESP32_FW_V2` BRANCH choice 只有 develop → 出 Jenkins 版要先 merge；**Jenkins 當時又掛了** → Alice 決定先給本地 build
- **本地 build 已打包**：`D:\mybot\release-binaries\mini2-esp32-ble-adv-fix-36aa375\`（4 bins + README + flash 指令，app md5 523FAFD8...）
- 驗證工具：scratchpad `ble2.exe`（WinRT active scan，顯示 AdvertisementType + scan rsp）；csc 編譯配方在本檔同 session 的 scratchpad build script 旁
- **已收案（17:11）**：fix build 已燒實驗機並空中複驗 LEN=8 ✅；zip 已打包 `release-binaries\mini2-esp32-ble-adv-fix-36aa375.zip`（md5 D235FDFD...），Alice 自傳 Acow
- 待辦：Acow 驗 looper → merge PR #2 → Jenkins `SPARK_ESP32_FW_V2` 出正式版（今日 Jenkins 兩度故障，出版前先確認）；詳見 sessions/mybot/2026-07-22-1711.md

---

## 2026-07-22 Spark MINI (Gen1) xloader 更新（已完成）
- 目標：這台 Gen1 Spark MINI 實機 bootloader 更新（Windows 25H2 patch）
- 燒入：BIAS_BOOTLOADER **#91 Boot_0_8_0_117.bin**（spark-10-develop-gd @ 9351835 = STFS-491 fix merge commit，含全部 5 修正；Jenkins artifact 與本地 byte-identical）
- 燒前備份：release-binaries/backup_MINI_bootloader_20260722_preflash.bin（原機 = 舊版 STM32-layout MSP 0x20004720，未含 patch，不匹配任何已知檔）
- 驗證：J-Link verify + 獨立 readback == 117 (True)；device_name G1JUV1 與原機一致（MODEL=G1JAMUP 才對，doc 裡 #86 G1MINI target 是早期錯誤 MODEL）
- Scripts: git/tool/{backup,flash}-spark-mini-bootloader-20260722.jlink, verify-mini-post-20260722.jlink
- 待做：按 power button 開機確認 app 正常（USB 295D:0212）

- 補充驗證 (16:xx)：實機 app 區 0x08008000 dump == SparkMINI_PSRAM_Burn-in_Test_GD_20260710_Setuptime_13.bin (byte-identical)。機內 = 117 bootloader + PSRAM burn-in app。SWD 一度不通是排線鬆脫（重插後 OK）。app 跑起來會佔 SWD，要 dump 需關機。
