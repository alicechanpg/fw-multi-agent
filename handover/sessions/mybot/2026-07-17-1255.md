# Session Handover (mybot) — 2026-07-17 12:15

> 本 session（`ebbd61cf`）只跑了 `/audit-review`，**未動 code / git / JIRA / 硬體**。
> 下方 FWP-814 是上個 session（7/16）留下、**至今仍未完成**的工作，原樣保留給下個 session。

## 本次（7/17）完成
- 跑 `/audit-review`（近 7 天），產出改善分析。三個可改行為，按嚴重度排序：
  1. **對外送出前未驗證**：上次送 Teo 的 GitHub URL 是壞的 → 送 Slack/JIRA 前一律用 `gh api ... html_url` 取權威 URL。
  2. **把訓練知識當硬體事實**：7/16 早上被反覆糾正（Spark2 有 esp32、DFU LED pattern、無 CDC、bootloader repo）；最貴一次是在未驗證 repo 上派了 2 個 subagent → 派 subagent 前先釘死 source-of-truth。
  3. **重找已知資訊 + 讀錯字硬做**（excel PID/VID 重搜、audio/audit 誤讀）→ 先讀 memory、短而含糊的請求先一句話確認。
- 對應 memory 多已存在（`feedback_verify_urls_before_sending` 等，7/16 晚上才建）；真正的測試是下個 session 有沒有照做。
- **待用戶確認**：是否新增一條 memory「派 subagent 前先釘死 source-of-truth repo」（現有 evidence memory 未明講此點）。

## FWP-814 carryover（上個 session 未完成，仍有效）

### 已完成（7/16）
- **Root cause**：audio anchor 假設所有 audio chip 都是 PG VID。`findSparkPid()` 只認 `vid == kUsbVid (0x295d)`，`getSparkLiveConfig().audioPid = "0221"`。Spark LIVE audio 其實是 C-MEDIA `0d8c:0033` → 比對不到 → fallback 回 Reactor 預設。
- **Fix `938239e`（已 push）**：audioPid 改 `"vid:pid"` 格式 + PG VID 優先 + 先驗 Reactor BT Audio（避免便宜 USB DAC 被當 LIVE 蓋過 Reactor）。

### 未完成 / 進行中
| 項目 | 狀態 | 下一步 |
|------|------|--------|
| Jenkins #32 (`938239e`, SIGN=false, TRIGGER_OSX=true) | building（7/16 觸發，結果待確認） | 查結果；要上機安裝需重打 `SIGN=true` |
| **LIVE `0d8c:0033` 實機驗證** | **未驗證** | 只做過 ARM GCC syntax check，需 LIVE 在線（現接的是 Reactor 100） |
| **Spark EDGE `0251`** | **未驗證** | 與 LIVE `0221` 同為推測值，很可能同樣錯 → 拿 EDGE 實測 |
| C-MEDIA false positive | 已緩解非根除 | 一般 USB 音效卡在無 PG 裝置時仍可能被當 LIVE |
| tests/ 目錄 | untracked | 3 個 pre-existing 失敗（icon `.ico` vs `.png`、products.json 多 `all` key），與本次無關，未動 |
| Reactor DFU LED source vs 實機 | 待對齊 | 本機 reactor-external-loader = Power LED，實機 = WiFi+BLE 白閃 |

## 環境狀態
- branch `feature/FWP-814-audio-anchor` @ `938239e`（= origin）
- 硬體：**Reactor 100** 在線（`295D:0501` audio + `295D:0506` CDC + ESP32 `303A:1001`）；**無 LIVE / 無 EDGE**
- 無 host C++ compiler（VS2022 需 admin、MSYS2 gcc 壞）；只有 ARM GCC 12.3 可 `-fsyntax-only`；無 qemu

## 給下個 session 的備註
- **Jenkins 一律 `TRIGGER_OSX=true`**（用戶 2026-07-16 指示）；交付 build 用 `SIGN=true`
- Jenkins **#29 已不存在**（輪替刪除）
- audio anchor 的 VID **不一定是 295D** — `constants.hpp` 那句 "shared by all Positive Grid devices" 是錯的
- **對外送 URL 前先用 `gh api` 核對**（本次 audit 最嚴重的一條）
