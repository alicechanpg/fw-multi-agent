# Session Handover (mybot) — 2026-04-15 15:30

## Done (STFS-491: Spark MINI/GO Firmware Update Failure on Win 25H2)

### Bootloader Fixes (g1x-bootloader, feature/spark-go-develop-gd, 15 commits)
1. **FIFO overflow fix** — TX FIFO 1536→1024 words (exceeded 1280 HW limit) `usb_conf.h`
2. **TRDT 5→9** — GD32F470 FS PHY needs larger turnaround time `usb_dcd_int.c + main.c`
3. **OTG_HS peripheral reset** — hard reset before USB init `main.c`
4. **Soft disconnect/reconnect** — 30ms disconnect + 10ms connect for clean attach `main.c`
5. **wTotalLength 65→37** — CS MS Interface Header per USB MIDI 1.0 spec `usbd_midi_core.c`
6. **Remove undeclared EP opens** (0x81, 0x82, 0x01) — only keep descriptor-declared EP `usbd_midi_core.c`
7. **UMP Discovery Reply** — reply 0xF0 0x11 "I'm MIDI 1.0" to Win 25H2 MIDISRV `usbd_midi_core.c`
8. **UMP Stream Config Reply** — reply 0xF0 0x06 to Stream Config Request `usbd_midi_core.c`
9. **Periodic Active Sensing** — send 0xFE every 300ms on bulk IN `usbd_midi_core.c + main.c`
10. **SEGGER RTT debug logging** — full USB enumeration trace `main.c, usb_bsp.c, usbd_core.c, usbd_req.c, usb_dcd_int.c`

### Updater App Fix (SparkFwUpgradeLauncher, PGHWComm submodule)
- **source->stop() deadlock fix** — remove from MIDI callback (JUCE 7 self-join deadlock)

### Automated Debug Pipeline
- Jenkins API → download artifact → J-Link Commander flash → reset → RTT RAM dump → Python parse
- Can force bootloader via J-Link: write "GSeries " to 0x10000000 + reset
- USB register dump via J-Link mem32 commands
- WinMM API test via python (midiInGetNumDevs, midiInOpen, etc.)

### Verification Results
| Test | Result |
|------|--------|
| USB enumeration (RTT) | ✅ SET_CONFIGURATION + all descriptors |
| midiInGetNumDevs | ✅ returns 1, "Spark GO" |
| midiInOpen (python) | ✅ "Open done" |
| USB Device Tree Viewer | ✅ Problem Code 0, Power D0 |
| Updater auto-detect bootloader | ❌ JUCE layer not sending SysEx |

## Pending
| Item | Status | Next Step |
|------|--------|-----------|
| Updater JUCE probe | **BLOCKED** | WinMM API works but JUCE MidiInput::openDevice doesn't send SysEx. Debug JUCE internals |
| Isolate bootloader fixes | 未開始 | 逐一關掉 9 個修法確認哪些是必要的 |
| Product string "Spark GO" | Bug | usbd_desc.c 寫死 "Spark GO"，應該根據 G1_MINI define 改成 "Spark MINI" |
| 移除 debug code | 未開始 | RTT log、1s delay、BOUT log — production build 前移除 |
| 端到端測試 | 未開始 | updater → bootloader → MCU FW update → 驗證 |
| Win 23H2/24H2 regression | 未開始 | 確認修法不影響舊版 Windows |
| MR review | WIP | Teo review, target: master |

## Environment
- g1x-bootloader: `feature/spark-go-develop-gd` (commits 13e8eab..48e7c05, 15 commits)
- SparkFwUpgradeLauncher: `origin/release/1.11.2.75-spark-mini-ble-1.91`
  - PGHWComm submodule: local branch `fix/STFS-491-win25h2-midi-deadlock` (source->stop patch)
- Spark MINI 硬體: GD32F470ZG, J-Link S/N 601005536, SWD 2000KHz
- Spark MINI 目前: bootloader mode (v10 build #85, Boot_0_8_0_100)
- App firmware: `Checksumed_spark_10_speaker_GD32.bin` at 0x08008000
- MidiSrv: Disabled（需要重新 enable: `sc config MidiSrv start= auto`）
- Jenkins: BIAS_BOOTLOADER builds #71-#85 (G1MINI)

## Notes for next session
1. **MidiSrv 目前是 Disabled！** 下次開始前要 `sc config MidiSrv start= auto && net start MidiSrv`
2. **midiInOpen 的成功可能跟 MidiSrv 乾淨重啟有關** — 之前 MidiSrv 因為舊 binary 卡住後需要完全重啟才恢復
3. **JUCE 的 MIDI probe 不送 SysEx 的原因未知** — 可能需要 debug JUCE MidiInput::openDevice 在 Win 25H2 的行為
4. **Product string bug**: usbd_desc.c 寫死 "Spark GO"，沒有 #ifdef G1_MINI。所有 product 共用同一個 string
5. **IAR dongle 不匹配**: SafeNet SuperPro 配不上 IAR 8.2，bootloader 只能用 Jenkins build
6. **Jenkins BIAS_BOOTLOADER BRANCH 是 ChoiceParameterDefinition** 不接受自訂 branch
7. **完整自動測試指令在 JIRA STFS-491 comment 裡**（Jenkins API、J-Link script、python WinMM test）
8. **Bootloader MR**: https://git.positivegrid.com:8443/firmware/g1x-bootloader/merge_requests/new?merge_request%5Bsource_branch%5D=feature%2Fspark-go-develop-gd
