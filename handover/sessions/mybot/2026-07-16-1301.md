# Session Handover (mybot) — 2026-07-16 13:01

> ⚠️ 事後補寫的完整 audit：本 session 過程中未即時寫 handover log（缺失，已於結束時補上）。所有動作皆有可稽核痕跡（git / JIRA / Jenkins / memory / Slack），下方逐項列出。

## 本次完成

### FWP-814 ReactorFwUpdater — 多產品偵測 / DFU / 文案（branch `feature/FWP-814-audio-anchor` → PR #12，已 push origin）
| commit | 內容 | 檔案 |
|--------|------|------|
| `005b0c2` | manual-DFU 頁偵測連接機型（顯示該機型圖+文案，不再預設 Reactor）+ LED 文字 | CoreStates.cpp, CdcStates.cpp, Text.hpp |
| `c304da2` | driver-install 以 DFU 存在性 gate（新 `usb::anyDevicePresent`）→ 關機裝置不再空轉 | FwPrepStates.cpp, UsbDriverWrapper.hpp/_win/_mac |
| `b518be0` | per-model DFU LED 指示文字 | Text.hpp |
| `41ed5ec` | 3-case 裝置存在偵測（新 `usb::anyDeviceWithVid`, ESP32 0x303A）| FwPrepStates.cpp, UsbDriverWrapper.hpp/_win/_mac |

### Jenkins builds — `REACTOR_SPARK_FW_UPDATER_WIN_OSX`（BRANCH=feature/FWP-814-audio-anchor, SIGN=false, TRIGGER_OSX=false）
- **#26 SUCCESS**(005b0c2) / #27(c304da2) / #28(b518be0) / **#29**(41ed5ec，觸發時 building)

### JIRA FWP-814（以 Alice 帳號）
- comment **92238**：per-product DFU LED 行為對照表 + 改動 log
- comment **92239**：scope 抱怨（英式委婉口氣；就地編輯 2 次）

### Bootloader / LED 研究（3 subagent）
- Reactor bootloader = `reactor-external-loader`（Power LED PD10, ~500ms 慢閃）
- Spark 2 bootloader = `spark-speaker-gen2-external-loader`（GitLab `jamup-speaker`，本機無）
- `spark-pedal-external-loader` = **Spark Pedal**（≠ LIVE/EDGE；LIVE/EDGE device source 不在 workspace）

### 實機 USB 驗證（Alice PC）
- 關機：只有 audio `295D:0231` + COM `295D:0901`
- 開機：多出 ESP32 **`303A:1001`** → 驗證「ESP32=power on」；ESP32 VID `0x303A` 對 Reactor + Spark 2 皆成立

### Memory
- 新增 `reference_pg_usb_hub_architecture.md`（USB hub 架構 + 3-case 偵測 + Spark 無 CDC + ESP32 VID 驗證）
- 新增 `reference_dfu_bootloader_repos.md`（各產品 bootloader = *-external-loader）
- 更新 `MEMORY.md` 索引

### Slack
- DM 給 Alice（`U04NS4ZFW5R`）FWP-814 抱怨備份

### 其他
- 開場：查證 Windows 2026-07-15 夜間 23:14–23:22 為 **Windows Update 計劃性重開 3 次**（非當機；無 Kernel-Power 41 / 6008）

## 未完成 / 進行中
| 項目 | 狀態 | 下一步 |
|------|------|--------|
| Jenkins #29 (41ed5ec) | 觸發時 building | 確認結果；成功則下載 unsigned Win updater 實機驗證 3-case |
| Reactor DFU LED：source vs 實機衝突 | 待對齊 | 本機 reactor-external-loader = Power LED，實機 = WiFi+BLE 白閃 → 疑本機為舊版 / 或 ESP32 驅動 |
| Spark LIVE/EDGE device bootloader | source 不在 workspace | 需 clone 才能 source 驗證 LED（依用戶指示暫緩）|
| 設計 / PM 資源 | 已於 FWP-814 反映 | 等 PM / 設計回應 |

## 環境狀態
- branch: `feature/FWP-814-audio-anchor` @ `41ed5ec`（= origin，已同步）
- 硬體: Spark 2（VID 295D）測試中；電源變動（audio 常在，ESP32 303A 開機才出現）

## 給下個 session 的備註
- **ESP32 VID `0x303A`** 已實機驗證（Reactor + Spark 2）
- **Spark 2/EDGE/LIVE 無 CDC**，FW update 不走 CDC，靠 DFU（`0232/0222/0252`）
- **正常 build 先更新 ESP32**（PrepareUpdateCoreState）；**without-esp32(SKIP_ESP32)** 才走 CheckUsbDriverState 的 DFU 3-case
- 3-case 偵測：audio=接USB／ESP32 或 DFU=power on／只有 audio=沒 power on → "Cannot detect the device"
- ⚠️ `terminal-map.json` 的 `mybot.jira` 標 FWP-739，但本 session 實際工作是 **FWP-814**（建議更新 map）
