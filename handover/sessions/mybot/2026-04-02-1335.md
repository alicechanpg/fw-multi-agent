# Session Handover (mybot) — 2026-04-01 15:20

## Done
- **FWP-698 Bypass CAB SIM**: Code 完成，branch `feature/FWP-698-bypass-cab-sim` 已 push
- **FWP-749**: Mac build plan 寫到 JIRA
- **spark-pedal-fw build**: GCC 10.3 和 12.3 都能 build 成功
- **Bootloader 恢復**: clone spark-pedal-external-loader, build + 燒回 internal flash
- **JIRA 更新**: FWP-698 三個 comment（實作、flash 指令、debug 狀況）

## Pending — CRITICAL
| Item | Status | Next Step |
|------|--------|-----------|
| **Pedal crash loop** | App HardFault | 需要原廠 bootloader binary（見下方 Notes） |
| FWP-698 MR | Branch pushed | 修復 boot 後建 MR 給 Ziv |
| FWP-698 實機測試 | Blocked | 等 Pedal 修復 |
| Peach LED 調色 | Blocked | 等 Pedal 修復 |

## Environment
- spark-pedal-fw: `feature/FWP-698-bypass-cab-sim` @ bf4665a (pushed)
- spark-pedal-external-loader: `develop` @ 9e34cdc (已 clone + build)
- .cproject: 改成 12.3（未 commit，本機 workaround）
- Import headers: 加了 missing includes（未 commit）
- Internal flash: 有我們 build 的 bootloader（可能跟原廠不同）
- External flash: 有 GCC 10.3 build 的 app（develop 或 FWP-698）
- **Pedal 狀態: crash loop（bootloader OK, app HardFault）**

## Notes for next session
### Pedal 修復方案（優先度排序）
1. **問 Ziv 拿原廠 bootloader binary** — 燒到 0x08000000 即可：
   ```
   STM32_Programmer_CLI -c port=SWD -d original-bootloader.bin 0x08000000 -v -rst
   ```
2. **或從其他正常 Pedal dump bootloader**：
   ```
   STM32_Programmer_CLI -c port=SWD -r 0x08000000 0x20000 bootloader_backup.bin
   ```
3. **或嘗試進 DFU mode**：拔電源→按住某鍵→接電源（問 Ziv 具體按法）

### Crash 原因分析
- GCC 12.3 build 的 app 也會 crash（不是 FWP-698 code 問題）
- GCC 10.3 build 的 app 也會 crash
- 懷疑是我們 build 的 bootloader 的 QSPI 初始化跟原廠不同
- CFSR=0x00008601（IACCVIOL + BusFault STKERR）

### Build 相關
- 本機 build 用 headless (CubeIDE 1.16.1 + GCC 12.3) 產生 Makefile
- 再移除 -fcyclomatic-complexity 用 GCC 10.3 make
- 或直接用 headless build（但 binary 在板上 HardFault）
- Jenkins 用 CubeIDE 1.12.1 headless（最穩，但本機沒裝）

### 重要指令
```bash
# ST-Link flash app（不要用 -e all！）
STM32_Programmer_CLI -c port=SWD -el QSPI_H750XB_W25Q32_flashloader.stldr -d spark-pedal-fw.bin 0x90000000 -rst

# ST-Link flash bootloader
STM32_Programmer_CLI -c port=SWD -d spark-pedal-external-loader.bin 0x08000000 -v -rst

# DFU flash app
dfu-util -s 0x90000000:leave:mass-erase:force -d 295d:0262 -D spark-pedal-fw.bin

# Serial log
COM50 @ 921600
```
