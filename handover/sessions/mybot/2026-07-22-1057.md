# Session Handover (mybot) — 2026-07-22 10:50

## 本次任務（諮詢，無 repo 動作）
Alice 問：以 memory 了解，要在 GitHub 放什麼能**證明能力**。中途補三個約束：
**(1) 要找工作 (2) 目標自動化測試 (3) 不要雞湯**。後續再追問「哪個東西工程師**真的會下載來用**」。

## 本次結論（已用工具查證，非猜測）
查證了以下檔案**真實存在**，作為推薦依據：
- `.claude/rules/*`（evidence-based-analysis / registry-first / session-handoff 等 5 條）
- `handover/registry/facts.jsonl`（33KB）、`handover/scripts/registry_hook.py`、`registry_query.py`
- `.claude/skills/qa-log-patterns/`（`error-signatures.md` = 三級 regex 規則庫 + false-positive 排除；`stm32-boot.md` / `esp32-boot.md` = 預期開機序列）
- 公司味 repo：`fw-multi-agent`（remote = `github.com/alicechanpg/fw-multi-agent`）

**推薦（單一）**：把 `qa-log-patterns` 去識別化，抽成獨立的 serial/boot log 斷言工具（暫名 `logassert`）——
輸入 log + YAML 規則（預期開機序列 + 錯誤簽章 + FP 排除），輸出 PASS/FAIL + exit code 供 CI 用。
理由：零硬體可試跑、pytest+GitHub Actions 直接展示測試+CI 能力、現成 FP 處理細節（`bt ack:fail` 連 3 次才算等）
展現實戰判斷。**判定「工程師會下載來用」= 只有這個過關**；整套 multi-agent workspace / HIL rig 綁死環境不會被 clone；
`registry_hook` 會有人用但屬「LLM 防幻覺」受眾，非測試職缺主打。

**紅線**：不得把 workspace / registry / JIRA/PID/Jenkins token 推公開 repo（洩密）。

## 未完成 / 下一步（本 session）
| 項目 | 狀態 | 下一步 |
|------|------|--------|
| `logassert` demo repo | **未開工，等 Alice 決定** | 待答：(a) 放哪個 GitHub 帳號（**不要** alicechanpg 那個公司 repo）(b) 工具命名 |
| 動手內容（若批准） | 未做 | 起乾淨 public repo 骨架（CLI + YAML + sample logs + pytest + Actions）→ 從 `error-signatures.md` 去識別化搬規則 → 寫 README |

---

## ⚠️ 上一 session（09:41）仍 pending 的事 — 未完成，勿遺失
> 以下是 Spark MINI2 BLE 分析（FW 3.2.1.90 / iOS App 4.5.4.96，Looper 連不上）的**未結案**待辦，
> 本 session 未處理，保留在此以免被覆蓋掉：

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| 給 Acow 的回覆 comment | **已送出（2026-07-22），等 Acow 回覆** | 等她回覆後再決定下一步 |
| FW 3.2.1.90 ↔ commit mapping | 未做 | 確認測試 build 是否含 ESP32 端 `cb91660` 及之後修正 |
| 根因假說硬體驗證 | 未做 | 故障機 BLE sniff 讀 `0x5D29` 6 bytes + STM32 UART `0xA7` MAC reply 比對 |

**該 session 結論（已 code 驗證）**：非 SA-1497 length bug 重現（`ble.c:57/67` length fix 已在）；
根因假說＝WB2853 UART 交握時序 vs ESP32「開機+2s 只問一次」的 race（`app_general_process.cpp:108-110`、
`WB2853.cpp:620/665-667`），**待硬體驗證**。⚠️ rule `fw-spark-stm32-ble-architecture.md` 稱 WB2853「passive 無 driver」
與事實不符（Mini2 有完整 `WB2853.cpp/.h` driver），日後更新 rule 要留意。

## 環境 / 備註
- 本 session **未動任何 repo**、未 commit/push、未送 Slack/JIRA。
- 完整稽核：`sessions/mybot/2026-07-22-1050-audit.md`（4 prompts / 3 tool calls / 0 fail / output 27k）。
