# Session Handover (mybot) — 2026-07-18 07:35

> Session id `2217eef5…`。本 session 為輕量查詢，未動任何 code/repo。
> 承接事項（FWP-833 / Reactor BDMA 調查）本次未推進，原樣帶到下一棒。

## 本 session 完成
- 應 user 要求讀 Slack DM（D04NS89Q6BC，最近 20 則），整理「哪些訊息在等 Alice 回覆」並回報：
  - **等回覆（主要）**：07-18 04:11 每日 digest 的兩個 audit 系統建議 —— (1) `/improve` 加 append-only `metrics.jsonl` 量化記錄；(2) `/improve` DISCOVER 階段加 `/checkup` 式 skills/context 使用率自我體檢。等 Alice 回「好」或「先不要」（依規則在 Claude Code 視窗回，不在 Slack）。
  - **待確認（較舊）**：FWP-744 Alice 角色定位（僅測試 vs 接手開發），未找到已回覆紀錄。
  - FWP-814「獨立 app vs 統一 updater」判定已解（memory：unified 架構、PR #12）。
- 確認 `D:\mybot\.claude\pending-questions.md` 目前無 PENDING 項目。
- 修正 memory：Reactor updater cmd 走 CDC+DFU（SysEx over CDC），Reactor 裝置本身有 UART MIDI（`MidiCmdHandler.cpp`/`SerialMIDI.cpp`）。
- **完成 Reactor SPI DMA 調查（承接事項結案）**：判定 **(c) 不需移植** — Reactor SPI5 ESP32 命令路徑已是 GPDMA + non-cacheable buffer（`SPI_Command_Master.cpp:148` DMA、`spi.c:120–168` GPDMA1 CH6/CH7、`.dma_buffer`→0x24060000 SRAM4 32KB MPU NOT_CACHEABLE）；`_IT` 呼叫全在 `#if 0`。H7R3 無 D3 domain，Mini2 的 BDMA staging 前提不存在。不用 build。詳見 memory `project_reactor_spi5_already_dma.md`（含下個產品 SPI2 檢查清單）。
- 發現過時文件：`reactor-fw/.claude/rules/embedded/stm32-freertos.md` 的 non-cacheable 區寫 0x24071C00/1KB，實際 0x24060000/32KB（未改，要走 reactor-fw repo 流程）。

## 未完成 / 進行中
| 項目 | 狀態 | 下一步 |
|------|------|--------|
| Alice 回覆兩個 digest 建議 + FWP-744 角色 | 等 user | 收到回覆後執行對應動作（建 metrics.jsonl / 改 /improve / 更新 FWP-744 規劃） |
| FWP-833 PR #51 (spark-mini-ii-fw) | OPEN，等 Ken Lee merge | 追 merge 狀態；merge 後更新 JIRA |

## 環境狀態
- 無 build/flash/git 變更；工作目錄 D:\mybot（master，工作樹有既有未提交改動，非本 session 產生）。

## ⚠️ 關鍵風險（承接，不要盲抄）
- Spark MINI 2 = **STM32H750**；Reactor = **STM32H7R3** → chip-isolation 鐵律，不可假設 same solution。
- PR #51 綁死 H750（SPI6 在 D3 domain，僅 BDMA 可服務）；H7R3 domain/DMA 需先查證，可能 (c) 不適用。
- 2026-07-18 更正：Reactor updater cmd 走 **CDC + DFU**（SysEx over CDC）；Reactor 裝置本身**有** UART MIDI（`MidiCmdHandler.cpp`/`SerialMIDI.cpp`）。舊句「走 CDC 不走 MIDI」只對 updater 的 USB 傳輸層成立，memory 已修正。

## 給下個 session 的備註
- 開場若 user 回覆 digest 建議或 FWP-744 定位，直接據此動工，不用再問一次。
- 若要 build Reactor：AMP firmware job（非 updater），TRIGGER_OSX=true 預設。
