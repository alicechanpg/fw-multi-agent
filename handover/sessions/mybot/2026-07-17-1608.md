# Session Handover (mybot) — 2026-07-17

> 本 session（`bb45070e`）主線＝**spark-mini-ii-fw：app 調音量/切 preset 有 glitch、knob 沒有**。
> **狀態：probe 已上機實測。已量到 SPI 指令流量 → audio miss deadline 的因果同步（見「已實測結論」）。**
> **等 app 連上後補測 volume 路徑本身**（先前 app 因 major version=0 連不上，已 bump 到 3 = build #17，已燒）。
> 前一個 session（`ebbd61cf`，SOP 治理 P1）的交班**原樣保留在下半部，其「下一步」仍然有效未執行**。

---

## 下一步（下個 session 從這裡開始）

### A. 本 session 主線：燒 build #16 上機抓 `[GP]` log

- **韌體**：`spark-mini-ii-fw-0.1.1.62.bin`（Jenkins `SPARK_MINI_2_FW` **#16**，branch `feature/audio-glitch-probe`）
- **抓 log**：USART1 @ **921600**。每 1 秒一筆 `[GP]`。
- **怎麼判讀**（這組 log 的設計目的就是**不需要人耳分辨 click/dropout**）：

| 現象 | 結論 |
|---|---|
| 調 app 音量時 `audio_overrun` > 0 或 `over_budget` > 0 | **dropout**：audio task 沒趕上 725us deadline → 往「SPI 路徑讓 Realtime7 都跑不動」查 |
| `audio_overrun` = 0 但仍有雜音，且 `vol_step_max` 很大 | **click**：gain 不連續 → 往 VolumeRamp / 值本身查 |
| `spi_process_cmd` 的 `max` 很大 | SPI task 單次處理過久（使用者原假設） |
| `preset_switch` 的 `max` 很大 | preset 載入阻塞 |

- **對照組必做**：轉 knob（不該有 overrun）vs app 調音量（`app_evt` 會跳），比較同一組數字。

> ⚠️ **不可以在 audio hot path 塞 `DEBUG_OUT`**：`debug.cpp:4` `CFG_DEBUG_UART_TX_BY_DMA_ENABLED (0)` → 走 polling 版 `HAL_UART_Transmit(..., HAL_MAX_DELAY)`（`debug.cpp:45`）**全阻塞**，921600 baud 下一行約 0.5ms，而 audio frame 預算只有 725us。塞進去等於自己製造 glitch。probe 因此全部只讀 DWT CYCCNT + 累加 RAM 計數，印出來的事交給獨立的 `gpDumpTask`（osPriorityLow）。

### 仍未回答的問題（不影響抓 log，但有助縮小範圍）
1. Glitch 是 click 還是 dropout？（使用者說不確定 → 已改由上表的 log 自動判定）
2. App 調音量是拖 slider 連續調才 glitch，還是點一格也會？

### B. 前一 session 的主線（仍未執行）
**執行 P1 實作計畫**：`docs/superpowers/plans/2026-07-17-p1-source-of-truth-registry.md`（詳見下半部）。

---

## 本次（`bb45070e`）完成

專案：`D:\mybot\git\spark-mini-ii-fw`。架構＝ **ESP32 + STM32**（同 Spark 2，使用者中途確認）。
**產出**：branch `feature/audio-glitch-probe` @ `32446f8`；Jenkins **#16**=`0.1.1.62`、**#17**=`3.1.1.63`（major bump 後）。**尚未開 PR**。

---

## ⭐ 已實測結論（上機量到的，非推測）

### 1. Audio frame 預算本來就很緊 — 閒置就吃掉 66%
```
[GP]  audio_frame  n=1391  avg=479us  max=540us  over_budget=0
```
預算 **725µs**（32 frames @ 44.1kHz）。`n=1391/s` ≈ 理論 1379 → probe 校準正確。**閒置 avg 479µs = 66%，尖峰 74%，只剩 ~185µs 餘裕。**

### 2. Knob 路徑完全不影響 audio（對照組）
`app_evt=0`、`audio_overrun=0`、`over_budget=0`、`audio_frame` 與閒置**完全相同**。
且 `vol_step_max` 到 **320/1000**（0.32 的大跳階）仍無 glitch → **證明 VolumeRamp 吃得下大跳階，「跳太大」不是 glitch 成因。**

### 3. ⭐ SPI 指令流量 → audio miss deadline（因果同步，實測）
```
ble_status = 0x1                                    ← BLE 連上
[GP] audio_overrun=3   over_budget=1  max=748us     ← 破 725µs
[GP] spi_process_cmd   max=2146us                   ← baseline 才 ~550µs
get_main_fw_version / get_serial_number / get_main_fw_version
[GP] audio_overrun=17  over_budget=9  max=751us     ← 一秒 miss 17 次
[GP] spi_process_cmd   max=3489us                   ← 3.5ms
ble_status = 0x0                                    ← BLE 斷
[GP] audio_overrun=0                                ← 立刻恢復
```
→ **使用者原假設「SPI task 耗時太久」成立；glitch 是 dropout 不是 click。**
→ ⚠️ **但這是 BLE 連線時的查詢 burst，不是 app 調音量本身**（`app_evt` 全程 0，log 中無 `set_inout_volume`）。同一條 SPI 路徑，高度相關但尚未直接量到 volume 指令。

### 4. 未解的機制（下一步的核心）
`spiCmdTask` 只有 **AboveNormal**，`saiTask` 是 **Realtime7**，`configUSE_PREEMPTION=1` → **「SPI 慢」不該餓死 audio**。要找的是它慢的時候為什麼連 Realtime7 都跑不動：長時間關中斷 / 持鎖 / SPI DMA 等待 / XIP 指令抓取被卡。

### 5. Probe 的已知盲點
`vol_step_max` 只在該 channel **active** 時才會動：`AudioService.cpp:474`，`gt_active=false`（吉他沒插）→ 走 `guitar_ramp.reset()`，**不呼叫 `prepare_frame`** → probe 不觸發。要看 guitar 跳階**必須插吉他**。

### 0. glitch probe（新增，已 build 過）
- 新檔 `Source/Common/debug/glitch_probe.{h,cpp}`；`GLITCH_PROBE_ENABLED` 設 0 可整包編掉。
- 量測點：`audio_frame`（每 half-buffer DSP 耗時 + 超過 725us 預算次數）、`audio_overrun`（DMA flag 已 pending＝miss deadline）、`vol_step`（VolumeRamp target 最大跳階，定點數）、`spi_process_cmd`、`preset_switch`、`app_evt`（標記 app 來的指令供對照）。
- 開銷：每點 2 次 CYCCNT 讀取 + 幾個比較（< ~50 cycles，約 audio frame 預算的 0.02%）。
- 接線：`application.cpp` `GP_INIT()`（DWT 啟用）+ `GP_START_DUMP_TASK()`（osPriorityLow，每 1s dump）。
- **Jenkins 用 CubeIDE headless 讀 `.cproject`，不是 Makefile**。`.cproject` Debug config 是資料夾自動收錄（`sourcePath name="Source"`，exclude 清單無 glitch_probe）→ 自動編到。Makefile 那行是給本地 build 用的。
- **APP_DEBUG**：job **沒有** mode 參數；模式來自 `user_config.h:22` = `RUNNING_MODE_APP_DEBUG`，`.cproject` 無 `RUNNING_MODE` define 覆蓋 → `DEBUG_OUT` 有效（`debug.h:21` 條件涵蓋 APP_DEBUG）→ `[GP]` 會輸出。**已驗證**：#16 console 有 `arm-none-eabi-g++ "../Source/Common/debug/glitch_probe.cpp"`，且我改的 4 個檔 **0 warning**（全 build 145 warning 是既有 baseline）。

### 1. app vs knob(hw) 路徑差異（已驗證，附 file:line）

| | Knob (HW) | App |
|---|---|---|
| 入口 | InputService ADC → event flag | ESP32 → SPI6 → SysEx parse |
| Task | `vol_ctrl_task` `AudioController.cpp:265`，**Normal1**，event 驅動 | `spi_command_process_task` `BleCmdHandler.cpp:80`，**AboveNormal**，polling 每 5–10ms |
| 呼叫 | `set_input_volume(ch, v, from_hw=true)` `:296,:304` | `set_inout_volume_callback` `:881` → `set_input_volume(ch, v)`，`from_hw` **預設 false**（`AudioController.h:414`）|
| 回 notify app | 會（mixerview 開時 post `OBJ_OUTPUT_VOLUME`）`:788-798` | 不會 |
| 套 gain | 寫 `input_vol[ch]` → 同一個 `VolumeRamp` | **完全相同** |

**承重結論**：`set_input_volume`（`AudioController.cpp:755-799`）只寫一個 float 陣列，沒碰 I2C/DSP。實際套 gain 在 `audio_process_task` 的 `VolumeRamp`（`AudioService.cpp:83-109`），指數平滑（每 frame 收斂 2%）+ per-sample 內插，**兩條路徑共用**。
→ **glitch 不是音量值/ramp 造成的**，差異只可能在 SPI 那條路徑「順便做的其它事」。preset 也走 SPI，共同點成立。

### 2. 使用者的「SPI task 耗時太久」假設有矛盾點（已驗證）
`saiTask` = **osPriorityRealtime7**（`AudioService.cpp:165`），`spiCmdTask` = **AboveNormal**（`BleCmdHandler.cpp:59`），`configUSE_PREEMPTION=1`（`FreeRTOSConfig.h:62`）。
→ **SPI task 單純耗時搶不走 audio task 的 CPU**，會被直接 preempt。要造成 glitch 必須有別的機制。

### 3. 候選機制（**尚未驗證，不可當結論**）
- **XIP 卡死**（目前最能同時解釋 volume + preset）：`STM32H750IBTX_FLASH.ld:46` `FLASH (rx) : ORIGIN = 0x90000000` = 外部 QSPI NOR；`AudioService.o`／`BleCmdHandler.o` **都不在 ITCM 清單**（只有 25 個 DSP .o 被搬進 ITCM）。任何 QSPI 操作或 I-cache 被洗掉 → audio task 抓不到指令 → 連 Realtime7 都 stall。
  - **缺口**：尚未驗證 app *volume* 路徑到底有沒有碰 QSPI。preset 路徑（`switch_to_preset_slot` / `save_preset` → FatFs）較可能。
- 長 critical section / 關中斷
- SPI DMA 與 SAI DMA 的 bus 競爭

### 4. 兩個已被自己推翻的假設（**別再重查**）
- ❌「app 路徑有 `DEBUG_OUT(%f)`（`BleCmdHandler.cpp:883`）、knob 的 `ACTRL_DBG` 被編掉，所以只有 app 阻塞」→ **錯**：`AudioController.cpp:32` 有 `#define ENABLE_ACTRL_DEBUG`，`ACTRL_DBG` = `DEBUG_OUT`，**兩條都會 log**。
- ❌「`get_input_volume` 的 `index = input_vol/0.00778f` 是 uint8_t 且無 bounds check → 溢位讀爆表」→ **錯**：`VOLUME_TAPER_TOTAL_LEVEL=129`（`AudioController.h:61`），vol=1.0 → index=128 界內；兩條路徑都有 clamp（`BleCmdHandler.cpp:889`）。
  - ⚠️ 但**確實是潛在風險**：`get_input_volume`（`AudioController.cpp:1018`）無 bounds check，而 `test_factory_test.cpp:257` 有。目前靠上游 clamp 保護。未修（使用者規則：不自己修無關的 bug）。

### 5. ESP32 source path（使用者中途要的，已驗證）
來源：Jenkins `SPARK_ESP32_FW_V2` build **#4** console（用 `curl -u alice.chan@positivegrid.com:$(cat D:/mybot/git/tool/.jenkins-token)` 抓 `/consoleText`）。

```bash
git clone https://github.com/Positive-LLC/pg-spark-esp32-wifi-bt.git
cd pg-spark-esp32-wifi-bt && git checkout develop   # build#4 = 15fdf54c51062b1969bff2e539688576028f0971
git submodule update --init --recursive             # submodule 走 GitLab SSH: git@git.positivegrid.com:8022
```
- submodule：`import/PGHWComm`（`desktop/PGHWComm.git` @ `c66c68ba`，commit 訊息 "Revert PGHWComm to jamup_speaker branch for Mixer View support"）→ 其下再掛 `JUCE` @ `41a380e7`、`msgpack`（`jamup-speaker/jamup-msgpack-c.git`）@ `e9c2cdcd`。
- ⚠️ 主 repo 走 GitHub HTTPS，**submodule 走 GitLab SSH**，需要 GitLab SSH key。

---

## 未完成 / 進行中（本 session）

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| **app volume 路徑實測** | **未做**（app 之前連不上） | #17 已燒（major=3）→ 開機 → app 連上 → 抓 `app_evt` 會跳的那段 |
| **glitch 機制** | **症狀已確認 = dropout**；機制未定 | 查 SPI 慢時為何 Realtime7 也跑不動（關中斷/鎖/DMA/XIP） |
| **`version.h` major=3** | **僅 debug 用，不可 merge** | `32446f8`；develop 原本是 0.1 |
| **PR** | **未開** | branch 已 push；使用者未要求開 PR |
| Jenkins job config | **已改**（加了 branch 選項） | 備份在 scratchpad `mini2_config_backup.xml`；查完可移除該選項 |
| ESP32 repo clone | 路徑已給，**未實際 clone** | 需要 GitLab SSH key 才能拉 submodule |
| `get_input_volume` 無 bounds check | 已知、未修 | 潛在風險，非本次 glitch 成因 |

## 環境狀態（本 session 補充）
- `D:\mybot\git\spark-mini-ii-fw`：branch **`feature/audio-glitch-probe`** @ `84a8957`（已 push origin），從 `develop` `d4a4816` 開出
- ⚠️ **submodule 未 init**（`Import/*` 全空）。**不要在本地跑 `gen_makefile.py` / `build_cli.sh`**：generator 掃檔案系統，會把 `Import/` 的 17 個來源從 Makefile 刪光（本 session 踩到，已還原）。
- ⚠️ **本地 toolchain 壞的**：CubeIDE 只有 1.16.1（腳本寫死找 1.17.0），且其 g++ 缺 `bits/c++config.h` → 連沒改過的 `debug.cpp` 都編不過。**本地無法 build，一律用 Jenkins。**
- 近期相關 commit：`8a61b5c` Merge PR#48 **FWP-364 SPI Enhancement**（SPI queue/event 架構重寫）← 與本問題時間點相關，值得回頭看
- MCU：STM32H750IBTX，code XIP 自外部 QSPI NOR @ `0x90000000`
- Debug UART：USART1 @ **921600**（`Core/Src/usart.c:86`），polling 阻塞式
- 本 session 未動 JIRA、未 push、未碰硬體

## 給下個 session 的備註（本 session）
- **`DEBUG_OUT` 是全阻塞的**（polling UART），任何 audio hot path 的 instrumentation 都不能用它 — 這是本 session 最重要的一條。
- 使用者的 SPI 直覺方向對（volume+preset 共同點＝SPI），但**「耗時久」本身解釋不了**，因為優先權 Realtime7 > AboveNormal。要找的是「SPI 路徑讓 Realtime7 都跑不動」的機制。
- **`SPARK_MINI_2_FW` 的 `BRANCH` 是 Choice**：新分支要先 POST `config.xml` 加選項才能 build（POST 需 crumb + cookie jar），否則 400。本 session 已加 `feature/audio-glitch-probe`。
- **Mini 2 SWD 是 `ap=0`，不是 Reactor 的 `ap=1`**（用 ap=1 → `Access port is not valid`）。正好印證 repo CLAUDE.md「不可假設與 Reactor 共用設定」。
- **⭐ ST-Link 燒錄要用 Mini2 專屬 loader：`C:\Users\alice\Downloads\SparkMini2_NorFlash_STM32H750.stldr`**（Ken Lee 2026-07-17 Slack 提供，**已實測可讀**）。
  - **repo 根目錄那兩個 .stldr 對這板子無效**：`QSPI_H750XB_W25Q32_flashloader`（`product.yaml` 指定的、64KB×17）與 `W25Q32JV_STM32H750`（4KB×260）**erase 全部失敗**（UR / HOTPLUG 都試過）。ST-Link 連線本身正常（3.29V、ID 0x450）→ 是 loader 對不上，不是接線。
  - 驗證指令（非破壞性）：`-c port=SWD ap=0 mode=UR -el <stldr> -r32 0x90000000 0x20` → 正常會讀到 SP=`0x24080000` + reset handler `0x9000xxxx`（合法 vector table）。
  - **有了它就不用手動切 DFU mode**，可直接 `-d <bin> 0x90000000 -v`。
  - ⚠️ **這個 loader 目前只在 Downloads，沒進 repo** → 建議跟 Ken 確認來源後收進 repo 或 tool/。
- **app 拒連 major version = 0**：Jenkins 從 `Source/Applications/version.h` 讀 MAJOR/MINOR，只有 `GIT_COMMIT_COUNT` 是它自己填。job 的 branch 清單裡有個 `test/mock-fw-version-for-app` → 這坑不是第一次踩。
- **DFU descriptor 少報容量**：`@External Flash /0x90000000/16*064Ka` 只宣告 1MB，但 bin 是 1038KB。結尾 `a` 在 ST DfuSe 格式是「唯讀」→ 這就是為什麼每個產品的 DFU 指令都要帶 **`:force`**（同時跳過 writeable 與 segment 範圍檢查）。實測寫得進去（實體 flash 是 W25Q32 = 4MB）。
- DFU 燒完結尾必有兩個**良性**訊息：`Invalid DFU suffix signature`（bin 沒帶 suffix）、`Error during download get_status`（`leave` 後裝置立刻斷線）。看到 `File downloaded successfully` 就是成功。
- Jenkins console 用 `/consoleText` 比 `/console`（HTML）好抓。
- **preset 存的是 SDNAND 不是 QSPI**（`BleCmdHandler.cpp` include `SDNAND.h`）→ 原本「preset 載入直接卡 QSPI」的假說要修正；XIP 影響的是**指令抓取**，不是資料存取。

---
---

# ⬇️ 以下為前一 session（`ebbd61cf`）交班，原樣保留 — 其「下一步」仍有效未執行

> 本 session（`ebbd61cf`）主線＝**建立 SOP 治理系統**：P0 完成、設計經獨立 opus 稽核升 v2、P1 spec + 實作計畫就緒。
> **下個 session 可直接執行 P1 實作計畫**（見「下一步」）。
> FWP-814 是 7/16 留下、仍未完成的工作，原樣保留在最後。

---

## 下一步（下個 session 從這裡開始）

**執行 P1 實作計畫**：`docs/superpowers/plans/2026-07-17-p1-source-of-truth-registry.md`（705 行，5 個 TDD task，已 push `b9330b1`）

- 執行方式**待使用者選**：Subagent-Driven（每 task 一個新 subagent，推薦）或 Inline（本 session 分批 checkpoint）。
- 環境已驗證：**Python 3.13.3 + pytest 9.1.1 可用**；`handover/` 原無測試慣例 → 計畫會新建 `handover/tests/` + `conftest.py`。
- **計畫刻意不自動刪 memory 檔**（見下方安全前提）。

---

## 本次（7/17）完成

### 1. /audit-review — 3 個可改行為
1. **對外送出前未驗證**（送 Teo 的 GitHub URL 是壞的）→ 送 Slack/JIRA 前用 `gh api ... html_url` 取權威 URL。
2. **把訓練知識當硬體事實**（7/16 反覆被糾正；最貴一次是在未驗證 repo 上派 2 個 subagent）→ 派 subagent 前先釘死 source-of-truth。
3. **重找已知資訊 + 讀錯字硬做**。
- **關鍵發現**：這 3 條的 memory **早就存在**（evidence_based 建於 5/05、stop_searching 建於 3/26）卻照樣犯 → **行政控制（寫規則）無效，要靠工程控制（hook）**。這成為整個系統的理論支點。

### 2. P0 完成並驗證 — 可評估的 metric 層
- `handover/scripts/metrics.py`（新）+ `session-digest.py`／`audit-report.py`（改）。
- **驗證**：對 7/16 transcript 算出 output=1,623,358／cache_read=232M／turns=922／model=claude-opus-4-8，與手算逐項一致；py_compile 三檔過；dummy 空 Stop payload 實跑 exit=0。
- `metrics.jsonl` 已 backfill 2 筆真實 session。**下個 session 關機起 KPI 表自動長**。

### 3. 設計 v2（經獨立 opus 對抗稽核，Verdict: needs-changes）
- Spec：`docs/superpowers/specs/2026-07-17-sop-governance-p0-metrics-design.md` §7 是稽核修正。
- Google Doc（Alice 讀）：https://docs.google.com/document/d/1YmEIX0YqhWLlz66AnrIelwq7LG1d_tYJ2DO806sZKYc/edit
- 已 Slack Alice 兩則結論。
- **稽核抓到的承重缺陷**：
  - 貴 model 用錯位置：一次 opus 抽查 ≈ 直接用 opus 做完整件事 → **判斷移到「捕捉當下」**。
  - registry = **單點故障**（三層檢查共用同源，一筆過期資料一起錯）→ 每筆 fact 要 owner/provenance/scope/TTL。
  - cheap model **不知道自己不知道** → escalation 改**可觀察事件觸發**，非自評信心。
  - **不可逆動作（flash/Jenkins/push/DFU）100% 硬 gate**，不抽樣。
  - 改善層自己沒人審 + 被指標帶偏 → diff 需人審 + 可回滾 + 防作弊反向指標。
  - **KPI 修正（我原提案錯了）**：「escalation-to-user 下降」會被**沉默作弊** → 改用 **repeat-escalation rate + 固定抽樣錯誤率**。

### 4. Workload 分布量測 → **P1 GO**
報表：`handover/audit/workload-distribution.md`（28 session／1120 則真人 prompt）

| 證據（regex 抽取，無選擇偏差） | 跨≥2 session 重現率 |
|---|---|
| JIRA ticket（＝「問題」代理） | **22%** → 每次都是新問題（long-tail）|
| PID/VID（＝「事實」代理） | **71%**，90% 提及來自會重現的 |

→ **永遠不問同一個問題，但一直需要同一批事實**。capture ROI 成立，但要 **key 在事實上，不是問題上**。核心約 **50 個實體** → registry 是「一張表」不是知識庫。
> ⚠️ 誠實標註：實體重現 ≠ 同一事實被重複需要（代理指標）。keyword 類（90%）因清單人工挑選**有選擇偏差、不採信**。COM 樣本僅 5 次無訊號。
> ⚠️ 第一版量測有 word-boundary bug（`ble` 吃到 problem/possible），已修正重跑；勿引用舊的「98.7%」。

### 5. P1 溫啟動發現（改變風險評估）
稽核擔心「registry 空 → 使用者被永久課稅」。但盤點證明**事實已被捕捉過**（`reference_*.md` 14 檔 + 3 個錯置的）→ **頭部事實已在手，P1 主要是遷移不是從零問起**。

### 6. P1 spec + 實作計畫
- Spec：`docs/superpowers/specs/2026-07-17-p1-source-of-truth-registry-design.md`（`e2eeb13`）
- Plan：`docs/superpowers/plans/2026-07-17-p1-source-of-truth-registry.md`（`b9330b1`）
- **已定案的兩個決策（使用者選的）**：
  1. **捕捉驗證：opus 寫+自驗**，只在「衝突／高風險（flash/PID/DFU/不可逆）／信心不足」時回問使用者；其餘走週報 digest 事後抽看（防 approval fatigue）。
  2. **registry 取代 `reference_*.md`**（單一真相），避免兩份真相分歧（COM7/COM19 衝突的成因）。

### 7. P1 包袱盤點（已完成，未動手改）
- memory 67 檔：「證據/驗證」群 **7 檔＝同 1 條規則的情境切面 → 可 7 併 1**；「別停下來」3 併 1。（**SOP 整併不在 P1 範圍**，另案）
- **COM/PID 事實在 3 處衝突**（COM7 vs COM19）→ registry 以 **PID/VID 為 key**，COM 一律 `volatile:true` + probe。
- MEMORY.md 索引覆蓋 88%，**8 個孤兒檔沒進索引**（含 com19/com50/check_hardware_pid）。
- frontmatter **兩種 schema 並存**（頂層 `type:` vs 巢狀 `metadata:`）→ 遷移前要統一 parser。
- fw-multi-agent 結構包袱：雙層 `scripts/scripts/`、`audit/audit/`（成因＝sync 腳本 `cp -r src/ dest/` 巢狀，L37-40）；**無 .gitignore** → `__pycache__`/`TEST-SID.jsonl` 被 tracked；98.3% tracked 檔是 auto-handoff 噪音。

---

## ⚠️ 安全前提（P1 執行時必守）

**`C:\Users\alice\.claude\projects\D--mybot\memory\` 不在 git 版控 → 刪除不可回復。**
遷移順序不可跳：**先備份 → 逐檔完整讀取遷移 → 覆蓋率對照表證明每條事實都有對應 key → 全部 covered 才可刪 → 更新 MEMORY.md 指向 registry**。
分流不是全搬：`reference_google_drive_visibility` 這類偏好註記**不進 registry**；分流清單須經使用者確認。

## 未完成 / 進行中

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| **P1 實作** | 計畫就緒、未執行 | 選執行方式 → 跑 5 個 TDD task |
| **memory 遷移/刪除** | 未做 | 人工關卡，須備份 + 覆蓋率背書 |
| **SOP 整併（7併1/3併1）** | 未做 | P1 之外的另案 |
| **結構清理** | 未做 | 先加 `.gitignore` → 修 sync `cp -r` 巢狀 → `git rm --cached` 去雙層（開分支） |
| **P2/P3/P4** | 未設計 | P1 穩了再說 |

---

## FWP-814 carryover（7/16 未完成，仍有效）

- **已完成**：root cause = audio anchor 假設所有 audio chip 都是 PG VID；LIVE audio 實為 C-MEDIA `0d8c:0033`。Fix `938239e` 已 push。
- **未完成**：

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| Jenkins #32 (`938239e`) | 7/16 building | 查結果；上機安裝需重打 `SIGN=true` |
| **LIVE `0d8c:0033` 實機驗證** | **未驗證** | 需 LIVE 在線（現接 Reactor 100） |
| **Spark EDGE `0251`** | **未驗證** | 與 LIVE `0221` 同為推測值，很可能同樣錯 |
| C-MEDIA false positive | 已緩解非根除 | 無 PG 裝置時一般 USB 音效卡仍可能被當 LIVE |
| Reactor DFU LED source vs 實機 | 待對齊 | 本機 external-loader = Power LED，實機 = WiFi+BLE 白閃 |

## 環境狀態
- `D:\mybot` = `Positive-LLC/FWP-713-fw-agent-dev`（公司 org，master）；**從未追蹤 handover/**
- `D:\mybot\fw-multi-agent` = `alicechanpg/fw-multi-agent`（**個人 repo，handover + 本專案文件的家**，main）
- ReactorFwUpdater branch `feature/FWP-814-audio-anchor` @ `938239e`
- 硬體：**Reactor 100** 在線；無 LIVE / 無 EDGE
- Python 3.13.3、pytest 9.1.1

## 給下個 session 的備註
- **對外送 URL 前先用 `gh api` 核對**（本次 audit 最嚴重的一條）
- **bash 裡不要用 PowerShell heredoc `@'...'@`**——本 session 因此讓 commit subject 多了個 `@`（已 amend 修掉）。多行字串用 `printf`。
- **console 是 cp950**：腳本不要把中文印到 stdout；報表寫檔用 Read 讀。
- **Jenkins 一律 `TRIGGER_OSX=true`**；交付 build 用 `SIGN=true`
- audio anchor 的 VID **不一定是 295D**
- 本 session 未動 JIRA（此工作非 ticket 驅動）；Slack/Atlassian MCP 中途曾斷線
