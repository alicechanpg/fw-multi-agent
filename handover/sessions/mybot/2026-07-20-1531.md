# Session Handover (mybot) — 2026-07-20 15:30

> Session id `cb9bf117…`。單一任務 session：在 `d:\gemini\` 安裝 Google Gemini CLI。
> 對外變更：無（無 git push / 無 JIRA / 無 Slack / 無 build/flash）。只在本機 `d:\gemini\` 新增本地 npm 安裝。

## 本 session 完成
- **安裝 Gemini CLI 到 `d:\gemini\`**（本地 npm 安裝，非 global）：
  - 套件 `@google/gemini-cli@0.51.0`，engines 需 Node `>=20`，實機 Node **v22.15.0** ✓。
  - 流程：`npm init -y` → `npm install @google/gemini-cli`（於 `d:\gemini\`）。
  - **實跑驗證通過**：`node d:\gemini\node_modules\@google\gemini-cli\bundle\gemini.js --version` → `0.51.0`。
  - bin shim 在 `d:\gemini\node_modules\.bin\gemini(.cmd/.ps1)`；bin 入口是 `bundle/gemini.js`（**不是** `dist/index.js` — 初次猜錯路徑，已用工具更正）。

## 未完成 / 進行中 / 待你決定
| 項目 | 狀態 | 下一步 |
|------|------|--------|
| **Gemini CLI 加入 PATH** | 未做（等 Alice 決定） | 若要全域用 `gemini`，把 `d:\gemini\node_modules\.bin` 加進使用者 PATH |
| **Gemini CLI 首次認證** | 未做（互動式） | Alice 自己在終端機跑 `! d:\gemini\node_modules\.bin\gemini`，走 Google OAuth 或設 `GEMINI_API_KEY` |
| **確認「gemini」語意** | 已假設=Google Gemini CLI | 若指別的工具需回頭改裝（已在回覆標為待確認） |

### 從上個 session 沿用、仍未關的項目（非本 session）
| 項目 | 狀態 | 下一步 |
|------|------|--------|
| jenkins.sh 實機驗證 | 未驗 | 真機 Git Bash 跑 `bash handover/scripts/jenkins.sh jobs`，確認 auth+crumb |
| registry P3（opus 每-turn 稽核） | 提案中 | 推進 P3 或先讓 P2 收 signal |
| fail_rate 盲區 | 未做 | 評估把 subagent `status=failed` 納入 `metrics.py` |
| FWP-814（原主線） | scope=Spark 2/LIVE/EDGE/MINI 2 | MINI 2 確認 PID 組合與更新路徑 |

## 環境狀態
- 本 session 無 build/flash、無 git 操作。`D:\mybot` 工作樹既有未提交改動為先前遺留，非本 session 產生。
- 新增本機目錄 `d:\gemini\`（含 `node_modules`, `package.json`, `package-lock.json`）。

## 給下個 session 的備註
- Gemini CLI 已裝好但**尚未認證**，Alice 要先跑一次登入才能用。
- 待決定事項（PATH / 認證）一律送 Alice Slack DM（U04NS4ZFW5R）。
