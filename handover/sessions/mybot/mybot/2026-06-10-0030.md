# Session Handover (mybot) — 2026-06-10 00:30

## Done
- Fixed `ota_stress_test.py` BLE notification parsing: raw byte check → full PGSysEx protocol parsing (f0 01 [SID] [CHK] [OP_MSB] [OP_LSB] [DATA_7BIT] f7)
- Added `decode_midi_7bit()` function for 7-bit MIDI encoding/decoding
- Added PGSysEx constants: ACT_DOWNLOAD=0x03, OBJ_FW_UPDATE_STATE=0x41, OBJ_OTA_PROGRESS=0x42
- Fixed cycle timeout: added `ota_terminal` asyncio.Event + `asyncio.wait(FIRST_COMPLETED)` for disconnect OR terminal state, with 10s grace period
- Fixed result classification: terminal state received = PASS (regardless of timeout)
- 1-cycle stress test verified PASS: START → progress 10%-100% → ST_CHECK_FW_FAIL → PASS (disconnect 10012.8ms after terminal state)

## Pending

| Item | Status | Next Step |
|------|--------|-----------|
| 100-cycle overnight stress test | Not started | Run `python ota_stress_test.py --cycles 100 --skip-initial-flash --no-serial` (~2.5 hrs) |
| ota_stress_test.py not committed | Untracked in D:\mybot\git\tool\ | Commit to tool repo |
| ble.c GATTS_CONNECTED_CLIENT_MAX_NUM=2 | Uncommitted on ESP32 branch | Decide: commit to branch or revert before merge |
| NVS cache shows "Reactor 100 BT Control" | Cosmetic | `esptool.py erase_region 0x9000 0x6000` to clear NVS if needed |

## JIRA Ticket Relationships
- **RAD-1454**: "[FW] Confirm OTA `.finished` is delivered before reboot" — the task being worked on
- **FWP-809**: ESP32 branch name (`fix/FWP-809_ota_finish_cannot_receive`) — the branch used for testing
- **FWP-739**: mybot workspace default ticket

## Environment
- mybot branch: master
- ESP32 branch: `fix/FWP-809_ota_finish_cannot_receive` (up to date with origin)
- ESP32 uncommitted change: `ble.c` GATTS_CONNECTED_CLIENT_MAX_NUM 1→2
- Hardware: Reactor 50 (R50)
- COM19: ESP32 serial (shared with flash)
- COM3: USB Relay (relay_hold_on.py PID 25120 running, relay ON = powered)
- relay state: ON (Reactor powered)

## Notes for next session
- **ST_CHECK_FW_FAIL is expected**: OTA downloads to ota_1 partition, STM32 firmware check fails (no real STM32 update), but the BLE notification WAS received — that's the PASS condition for RAD-1454
- **Per-cycle reset**: After OTA completes (device on ota_1), erasing otadata (0xd000, 8KB) + hard reset boots back to ota_0 — no full reflash needed (~91s per cycle)
- **Auto OTA simulation**: `ENABLE_APP_CMD_SIMULATION=1` in `app_https_fw_ota.cpp:14` auto-triggers OTA after boot
- **relay_hold_on.py**: Don't kill PID 25120 — it keeps COM3 open with DTR=True to maintain Reactor power
- **ota_stress_test.py location**: `D:\mybot\git\tool\ota_stress_test.py` (untracked, needs commit)
