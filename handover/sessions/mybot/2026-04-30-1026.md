# Session Handover (mybot) — 2026-04-29 17:15

## Done — STFS-491 PC patches ship + 25H2 hang verified + 25H2 WinMM regression discovered

### 設計文件 commit + push 進 git baseline (mybot master @ `83021c1`)
- 3 份 design doc + README → `D:\mybot\docs\STFS-491-{system,pc-coding,gen1-coding}-design.md` + `STFS-491-README.md`
- Internal baseline，不需 Teo review (user 確認)

### STFS-491 完整 PC patch line-up — 全 ship

| Patch | File | Repo | Commit |
|-------|------|------|--------|
| **B1~B8** | g1x-bootloader | g1x-bootloader | Jenkins #86 (2026-04-16) |
| **P1** midiInClose retries 5→50 | juce_win32_Midi.cpp | JUCE PG fork | `79392d03` |
| **P2** sendMessageNow 5s timeout + midiOutReset | juce_win32_Midi.cpp | JUCE PG fork | `e57b2b5b` |
| **P3** ConnectedAppState → std::thread | ConnectedAppState.cpp | Launcher | `92e7b4c` |
| **P4** Drop callFunctionOnMessageThread | PGUsbMidiDeviceFinder.cpp | PGHWComm | `fd837a3` |
| **P5a** ConnectingAppState connect() off message thread | ConnectingAppState.cpp | Launcher | `598ae04` + `14faaa3` (fixup) |
| **P5b** setOnDeviceListChangedCallback (2 sites) → std::thread + atomic_flag | SparkMiniFwUpdatingAppState.cpp | Launcher | `e9db692` |
| **P6** PGDeviceManager::Impl::timerCallback off message thread | PGDeviceManager.cpp | Launcher | `04caf8f` |

Branches:
- JUCE: `fix/STFS-491-win25h2` @ `e57b2b5b`
- PGHWComm: `fix/STFS-491-win25h2-midi-deadlock` @ `fd837a3`
- Launcher: `fix/STFS-491-spark-mini-win25h2` @ `04caf8f`

### Jenkins builds（同 `JAMUP_SPEAKER_FW_TOOL_WIN`）
- #247 (P1~P4): compile clean，**hang 重現**
- #248 (+P5): compile fail (forward-decl `pg::PGDevice*` 不能 lambda capture)
- #249 (+P5a fixup): compile clean，**hang 仍重現**（P5 修下游、上游 timer 沒解）
- **#250 (+P6)**: compile clean，**Updater 連續 30s+ Responding=True 不卡** ✅
- 全 build 標 FAILURE 都是 signtool cert infra（IT 待修），artifact 完整可用

### 25H2 端到端測試結果

**做到的（P5+P6 verify）**：
- Updater on Win 11 build 26200 連續 30s+ polling Responding=True，不再卡 LpcReply ✅
- J-Link 強寫 `"GSeries "` magic + reset → GD32 進 bootloader → USB enumerate `VID_295D PID_0202` ✅
- MidiSrv 註冊 3 個 KSA endpoints (`MIDIU_KSA_5615...`) ← B7 UMP Discovery Reply 生效 ✅
- 物理拔 USB → 重接 → device 救回 normal mode ✅

**做不到的（新發現的 25H2 OS-level regression）**：
- Win 11 25H2 上 `midiInGetNumDevs() = 0` — legacy WinMM 看不到任何 USB MIDI input
- `midiOutGetNumDevs() = 1` (只 Microsoft GS Wavetable Synth)
- Spark MINI normal-mode MIDI 在 PnP 看得到 (`USB\VID_10D6&PID_0201&MI_01` MEDIA class) 但**不在 WinMM 列表**
- Haute Technique virtual MIDI driver 也不在 WinMM 列表（影響非 Spark-only）
- 推測雙層成因疊加：(a) 25H2 OS WinMM input enum regression (b) Spark MINI App firmware 沒 reply UMP Discovery (B7 fix 只 cherry-pick 到 bootloader，App firmware 不在 STFS-491 scope)
- ⚠️ 沒做 Korg 對照測試確認 (a) 是 OS-wide 還是 device-specific

### Spark MINI device rescue（session 中段）
J-Link 強制 trigger bootloader 後 Actions BLE chip 卡 ADFU mode (VID_10D6 PID_10D6)。J-Link 摸不到 Actions chip。User 拔 USB 等 30s 重接 → 救回 normal mode。

### JIRA 三則 update comments
- **#87612** Root cause + P5/P6 + Build #250 25H2 survived 完整報告
- **#87613** Session handover brief (16:35)
- **#87635** 25H2 WinMM regression finding + STFS-491 收尾建議
- 全 link: <https://positivegrid.atlassian.net/browse/STFS-491>

## Pending

| 項目 | 狀態 | 下一步 |
|------|------|--------|
| **STFS-491 ticket 收尾** | hang fix 完成，端到端 normal-mode detection 卡 25H2 OS issue | 待 PM/Teo 決定 acceptance criteria 是否包 normal-mode detection |
| **新 ticket: 25H2 normal-mode detection** | 建議開但**未開** | 等 PM/Teo 決定 scope（方向 1: App fw UMP Reply / 方向 2: Updater migrate to MIDI 2.0 API）|
| **Korg 對照測試** | 未做 | 找一台 known-good USB MIDI device 插 25H2 機器，跑 `midiInGetNumDevs()` 看是 0 還是 ≥ 1 |
| **完整 BLE+MCU update flow 端到端** | 25H2 上做不到（被 detection issue block） | 等新 ticket 解後重做 |
| **Win 10 / Win 11 23H2 regression** | 待測 | P6 改 timer 行為，舊 OS 應同行為但需 regression |
| **3 個 PR 開** | 待 | Order 1 (JUCE) → Order 2 (PGHWComm) → Order 3 (Launcher)，PG GitLab MR |
| **Signtool cert infra issue** | 通報 IT | 跟 STFS-491 無關 |
| **Doc update**: 把 P5/P6 finding + WinMM regression 加進 design docs v3 | 待 | 整理進 system-design / pc-coding-design |
| **FWP-744 Mac validation** (前 session) | 等 Mike Lee (4/30) | 等他 reply Slack |

## Environment

### Branch / commit 狀態
- `D:/mybot` master @ `83021c1` (pushed)
- `D:/mybot/git/SparkFwUpgradeLauncher`: `fix/STFS-491-spark-mini-win25h2` @ `04caf8f` (pushed)
- `D:/mybot/git/SparkFwUpgradeLauncher/PGHWComm`: `fix/STFS-491-win25h2-midi-deadlock` @ `fd837a3` (pushed force-with-lease)
- `D:/mybot/git/SparkFwUpgradeLauncher/PGHWComm/import/JUCE`: `fix/STFS-491-win25h2` @ `e57b2b5b` (pushed PG fork)

### Hardware
- Spark MINI 接著 normal mode (PID_0201)，App firmware running，綠燈呼吸
- Reactor 50 也接著（無關 STFS-491）
- J-Link V11 (S/N 601005536) 連 Spark MINI GD32 SWD
- COM Port: COM4 STLink VCP / COM3 USB Relay / COM15 R50 CDC (PID 0507) / COM19 ESP32

### Updater process
- PID 6544 還在 (Spark MINI Firmware Updater)，Responding=True
- 卡在 InitialAppState 不動（等 device 從 WinMM 出現，但 25H2 看不到）

### 重要檔案
- Jenkins #250 binary: `D:\mybot\git\tool\release-binaries\STFS-491-SparkMINI-Updater\Spark_MINI_Firmware_Updater_Win-250.zip`（解壓在同目錄）
- Bootloader binary: `D:\mybot\git\tool\release-binaries\STFS-491-Boot_MINI_GD32_86.bin`
- Design docs: `D:\mybot\docs\STFS-491-*.md`

## Notes for next session

### 立即可做
1. **找 Korg / 任何 USB MIDI device 插 25H2 機**，驗證 `midiInGetNumDevs()` 是否真的全 0 — 確定是 OS regression 還是 Spark MINI specific
2. **與 PM/Teo 對齊** STFS-491 acceptance criteria（hang fix 還是端到端 update flow）
3. **如果 acceptance 是端到端**：新 ticket 要先決定方向（App fw UMP / Updater migrate to MIDI 2.0）

### 大方向決策（給 PM/Teo）

**Scenario A — Korg 也看不到（確認是 OS regression）**：
- App firmware 加 UMP Reply 沒用（OS 已壞）
- 必須 Updater migrate to Windows MIDI Services 2.0 API
- 工程量大（跨 GO/40/NEO Updater），但長期治本
- 短期可接受？：等 Microsoft 25H2 hotfix 修 WinMM enum，先 ship 現狀（hang fix 已解，bootloader path 走得通用 J-Link / 出貨工廠燒）

**Scenario B — 只 Spark MINI 看不到**：
- App firmware UMP Reply (B7 移植) 即可解
- 工程量小，沿用 bootloader 已驗證的 reply payload
- 但要找 Spark MINI App firmware team review

### 6 個 patch 完整 line-up
B1~B8 (Gen 1) + P1+P2 (JUCE) + P3+P4+P5a+P5b+P6 (Launcher/PGHWComm/JUCE) = 14 個 fixes 全 ship 並 25H2 verified（限 hang fix scope）

### 重要陷阱
- **VS2017 lambda capture** 不認 forward-declared pointer type → 用 bool 容器
- **Jenkins ChoiceParameter** 嚴格 enforce list → 改 config.xml POST 加 branch
- **`git submodule update`** reset working tree → multi-stage push 中間別跑
- **Spark MINI Actions chip ≠ GD32** — J-Link 只能 reset GD32，Actions chip 要物理斷電
- **`juce::Timer`** 永遠 message thread → 任何 inherit Timer 的 timerCallback 內不能 blocking I/O
- **`pg::DeviceManager::Impl::m_inputDevices` 是 stale cache** — disconnect() 不清，可能造成 callback 不 fire（見 candidate A 分析）
- **`midiInGetNumDevs()` 在 25H2 對 USB MIDI device 回 0** — legacy WinMM enum 似乎 broken (待 Korg 對照)
- **物理 USB 拔再插**是最可靠的 25H2 device rescue 法

### Subagent 討論記錄（per AI Collab rule）
- P5 patch design 2 輪 review (atomic_flag re-entry guard, capture types, commit split)
- P6 是現場找出 root cause 後直接 ship（subagent 沒參與 — 因為證據明確：thread state、source code 對照）
- WinMM regression finding subagent reviewer 要求降級措辭、加 caveat、刪 J-Link workaround 細節 — 全採納
- 三則 JIRA comment 都過 subagent review

### 工具註記
- Jenkins API auth: `alice.chan@positivegrid.com:<token>`
- Jenkins config xml POST 立即生效
- `JLinkRTTLogger.exe` 找不到 RTT control block — Spark MINI bootloader cherry-pick 自 GO 時 RTT 部分被 conflict 解掉
- WinMM enum 查 PowerShell: 直接 DllImport `winmm.dll!midiInGetNumDevs` + `MIDIINCAPS` struct
- Updater 卡 hang 看 `Get-Process | %{$_.Threads}` ThreadState/WaitReason — `LpcReply` = MidiSrv RPC stuck signature
